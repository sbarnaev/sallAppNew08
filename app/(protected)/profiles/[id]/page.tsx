"use client";

import { useEffect, useMemo, useState, useRef, useCallback } from "react";
import { useParams } from "next/navigation";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import html2pdf from "html2pdf.js";

// Интересные факты для отображения во время генерации (200 фактов)
const INTERESTING_FACTS = [
  "🧠 Знаете ли вы, что мозг человека генерирует около 70 000 мыслей в день?",
  "💫 Системный анализ личности основан на древних знаниях о циклах и архетипах",
  "🌟 Каждый человек уникален, но все мы следуем определенным паттернам развития",
  "📊 Психологи утверждают, что понимание себя — первый шаг к успеху",
  "🎯 Целеустремленность увеличивает вероятность достижения цели на 42%",
  "🤝 Коммуникация составляет 93% нашего взаимодействия с миром",
  "💡 Интуиция часто работает быстрее логического мышления",
  "🌱 Личностный рост — это непрерывный процесс, а не конечная точка",
  "🎨 Творчество помогает находить нестандартные решения проблем",
  "⚖️ Баланс между разными аспектами личности — ключ к гармонии",
  "🔮 Самопознание открывает двери к новым возможностям",
  "🌟 У каждого есть уникальные таланты, которые ждут своего раскрытия",
  "💪 Сила воли, как мышца, укрепляется через практику",
  "🌊 Эмоции — это волны, которые можно научиться управлять",
  "🎭 Мы играем разные роли, но важно оставаться собой",
  "🔍 Глубокое понимание себя помогает лучше понимать других",
  "📈 Небольшие ежедневные изменения приводят к большим результатам",
  "🎯 Ясность целей увеличивает мотивацию в 3 раза",
  "🤔 Рефлексия — мощный инструмент для личностного развития",
  "✨ Каждый день — новая возможность для роста и изменений",
  "🧬 Наш мозг способен создавать новые нейронные связи на протяжении всей жизни",
  "💭 95% наших решений принимаются подсознательно",
  "🌙 Сон помогает мозгу обрабатывать и систематизировать полученную информацию",
  "🎪 Позитивное мышление может увеличить продолжительность жизни на 7-10 лет",
  "🔐 Привычки формируются в среднем за 66 дней",
  "📚 Чтение развивает эмпатию и улучшает социальные навыки",
  "🎵 Музыка активирует почти все области мозга одновременно",
  "🧘 Медитация увеличивает плотность серого вещества в мозге",
  "🏃 Физические упражнения стимулируют выработку эндорфинов — гормонов счастья",
  "🌿 Природа снижает уровень кортизола — гормона стресса",
  "🤗 Объятия продолжительностью 20 секунд повышают уровень окситоцина",
  "😊 Улыбка, даже искусственная, может улучшить настроение",
  "💬 Разговор с самим собой помогает лучше справляться со стрессом",
  "🎯 Визуализация цели активирует те же нейронные пути, что и реальное действие",
  "📝 Ведение дневника благодарности повышает уровень счастья на 25%",
  "🔄 Нейропластичность позволяет мозгу перестраиваться после травм",
  "🎓 Обучение новому навыку создает новые нейронные связи",
  "🧩 Решение головоломок замедляет старение мозга",
  "🌅 Утренний солнечный свет регулирует циркадные ритмы и улучшает сон",
  "💧 Обезвоживание всего на 2% снижает когнитивные способности",
  "🍫 Темный шоколад улучшает кровоснабжение мозга",
  "🧠 Мозг использует 20% всей энергии организма, хотя весит всего 2%",
  "🎨 Творческие люди чаще используют оба полушария мозга одновременно",
  "💤 Недосып снижает способность к обучению на 40%",
  "🎪 Многозадачность снижает продуктивность на 40%",
  "📱 Постоянные уведомления снижают IQ на 10 пунктов",
  "🌊 Прогулка на природе улучшает креативность на 50%",
  "🤝 Социальные связи важнее для здоровья, чем отказ от курения",
  "💭 Мысленные тренировки могут улучшить физические навыки",
  "🎯 Постановка конкретных целей увеличивает шансы на успех в 10 раз",
  "📊 80% успеха зависит от эмоционального интеллекта, а не IQ",
  "🧘 Практика осознанности снижает тревожность на 60%",
  "🎵 Классическая музыка может улучшить концентрацию внимания",
  "🌱 Оптимисты живут в среднем на 15% дольше пессимистов",
  "💪 Регулярные физические упражнения увеличивают объем гиппокампа",
  "📚 Люди, читающие книги, живут в среднем на 2 года дольше",
  "🎨 Искусство и творчество активируют систему вознаграждения мозга",
  "🤗 Социальная поддержка снижает риск депрессии на 50%",
  "💡 Моменты озарения происходят, когда мозг находится в состоянии покоя",
  "🌙 Лучшее время для творческой работы — раннее утро",
  "🎯 Фокус на процессе, а не результате, снижает стресс",
  "🧬 Генетика определяет только 30% нашего счастья, остальное — в наших руках",
  "💭 Позитивные аффирмации могут изменить нейронные пути",
  "🎪 Смех снижает уровень стресса и укрепляет иммунитет",
  "🌿 Зеленые растения в помещении улучшают концентрацию на 15%",
  "📝 Записывание целей увеличивает вероятность их достижения на 42%",
  "🔄 Привычка формируется за 18-254 дня, в среднем 66 дней",
  "🎓 Обучение других помогает лучше запоминать информацию",
  "🧠 Мозг обрабатывает визуальную информацию в 60 000 раз быстрее текста",
  "💤 Качественный сон улучшает память и способность к обучению",
  "🎯 Разбиение больших целей на маленькие шаги увеличивает мотивацию",
  "🤝 Эмпатия можно развивать через практику и тренировку",
  "💡 Креативность повышается, когда мы расслаблены и не фокусируемся на проблеме",
  "🌊 Вода составляет 75% массы мозга",
  "🎵 Музыка может вызвать выброс дофамина, как наркотики",
  "🧘 Медитация увеличивает объем префронтальной коры",
  "📚 Чтение художественной литературы развивает теорию разума",
  "🎨 Творчество активирует дофаминовую систему вознаграждения",
  "🤗 Физический контакт снижает уровень кортизола",
  "💭 Визуализация успеха активирует те же нейроны, что и реальный успех",
  "🌱 Позитивное мышление укрепляет иммунную систему",
  "🎯 Конкретные цели мотивируют больше, чем абстрактные",
  "📊 Эмоциональный интеллект можно развивать на протяжении всей жизни",
  "🧠 Мозг продолжает развиваться до 25 лет",
  "💤 Сон помогает консолидировать память и закреплять обучение",
  "🎪 Перерывы в работе повышают продуктивность на 20%",
  "🌿 Природа восстанавливает внимание и снижает умственную усталость",
  "💧 Обезвоживание ухудшает настроение и когнитивные функции",
  "🍫 Антиоксиданты в темном шоколаде улучшают кровоток в мозге",
  "🧬 Нейрогенез — образование новых нейронов — происходит всю жизнь",
  "🎨 Творческие люди лучше справляются с неопределенностью",
  "💭 Медитация изменяет структуру мозга всего за 8 недель",
  "🏃 Физическая активность стимулирует рост новых нейронов",
  "📚 Обучение новому языку замедляет старение мозга",
  "🎵 Музыкальная терапия помогает при депрессии и тревоге",
  "🧘 Практика благодарности перестраивает нейронные пути",
  "🤝 Социальная изоляция так же вредна, как курение 15 сигарет в день",
  "💡 Моменты инсайта сопровождаются выбросом дофамина",
  "🌙 Циркадные ритмы влияют на когнитивные способности",
  "🎯 Система вознаграждения мозга активируется при достижении целей",
  "📊 Эмоции влияют на принятие решений больше, чем логика",
  "🧠 Мозг экономит энергию, создавая шаблоны и автоматизмы",
  "💤 Недосып нарушает работу префронтальной коры",
  "🎪 Многозадачность истощает запасы глюкозы в мозге",
  "🌿 Прогулки на природе снижают активность области мозга, связанной с депрессией",
  "💧 Вода улучшает когнитивные функции и настроение",
  "🍫 Флавоноиды в какао улучшают память и внимание",
  "🧬 Пластичность мозга позволяет адаптироваться к изменениям",
  "🎨 Творчество помогает справляться со стрессом и тревогой",
  "💭 Осознанность снижает активность миндалевидного тела",
  "🏃 Кардио-упражнения увеличивают объем гиппокампа на 2%",
  "📚 Обучение активирует систему вознаграждения мозга",
  "🎵 Музыка может синхронизировать мозговые волны",
  "🧘 Медитация увеличивает плотность серого вещества",
  "🤝 Социальные связи активируют систему вознаграждения",
  "💡 Креативность связана с повышенной активностью сети пассивного режима",
  "🌙 Качественный сон очищает мозг от токсинов",
  "🎯 Постановка целей активирует префронтальную кору",
  "📊 Эмоциональный интеллект коррелирует с успехом в карьере",
  "🧠 Мозг использует больше энергии, чем любой другой орган",
  "💤 Сон помогает обрабатывать эмоциональные переживания",
  "🎪 Фокус на одной задаче повышает качество работы",
  "🌿 Природа восстанавливает когнитивные ресурсы",
  "💧 Вода необходима для передачи нервных импульсов",
  "🍫 Какао улучшает приток крови к мозгу",
  "🧬 Нейропластичность позволяет мозгу восстанавливаться",
  "🎨 Творчество активирует множественные области мозга",
  "💭 Медитация изменяет экспрессию генов",
  "🏃 Физические упражнения повышают уровень BDNF",
  "📚 Чтение активирует воображение и эмпатию",
  "🎵 Музыка может снижать уровень кортизола",
  "🧘 Практика осознанности улучшает внимание",
  "🤝 Социальная поддержка снижает риск заболеваний",
  "💡 Инсайты происходят в состоянии расслабления",
  "🌙 Регулярный сон улучшает иммунитет",
  "🎯 Конкретные цели активируют мотивационные центры",
  "📊 Эмоции влияют на физическое здоровье",
  "🧠 Мозг создает воспоминания каждый раз, когда мы их вспоминаем",
  "💤 Сон помогает консолидировать долговременную память",
  "🎪 Перерывы улучшают креативность и продуктивность",
  "🌿 Зелень в помещении снижает стресс",
  "💧 Вода улучшает когнитивные способности",
  "🍫 Темный шоколад содержит теобромин, улучшающий настроение",
  "🧬 Новые нейроны образуются в гиппокампе",
  "🎨 Творчество помогает выражать эмоции",
  "💭 Осознанность снижает реактивность на стресс",
  "🏃 Упражнения повышают уровень серотонина",
  "📚 Обучение создает новые нейронные связи",
  "🎵 Музыка активирует центры удовольствия",
  "🧘 Медитация увеличивает объем серого вещества",
  "🤝 Социальные связи важны для выживания",
  "💡 Креативность связана с открытостью опыту",
  "🌙 Сон помогает обрабатывать информацию",
  "🎯 Цели мотивируют через систему вознаграждения",
  "📊 Эмоции помогают принимать решения",
  "🧠 Мозг адаптируется к изменениям среды",
  "💤 Качественный сон улучшает память",
  "🎪 Фокус повышает эффективность работы",
  "🌿 Природа снижает уровень стресса",
  "💧 Вода необходима для работы мозга",
  "🍫 Какао улучшает когнитивные функции",
  "🧬 Пластичность мозга позволяет учиться",
  "🎨 Творчество развивает гибкость мышления",
  "💭 Медитация улучшает эмоциональную регуляцию",
  "🏃 Физическая активность улучшает настроение",
  "📚 Чтение расширяет словарный запас",
  "🎵 Музыка может вызывать мурашки",
  "🧘 Практика осознанности снижает тревогу",
  "🤝 Социальная поддержка укрепляет здоровье",
  "💡 Инсайты происходят в моменты покоя",
  "🌙 Регулярный сон улучшает концентрацию",
  "🎯 Конкретные цели легче достигать",
  "📊 Эмоции влияют на восприятие времени",
  "🧠 Мозг обрабатывает информацию параллельно",
  "💤 Сон помогает закреплять навыки",
  "🎪 Отдых восстанавливает когнитивные ресурсы",
  "🌿 Природа улучшает психическое здоровье",
  "💧 Вода улучшает физическую работоспособность",
  "🍫 Шоколад содержит фенилэтиламин — молекулу любви",
  "🧬 Нейрогенез происходит в ответ на обучение",
  "🎨 Творчество помогает справляться с эмоциями",
  "💭 Осознанность улучшает самоконтроль",
  "🏃 Упражнения повышают уровень эндорфинов",
  "📚 Обучение активирует дофаминовую систему",
  "🎵 Музыка синхронизирует сердечный ритм",
  "🧘 Медитация изменяет структуру мозга",
  "🤝 Социальные связи снижают риск деменции",
  "💡 Креативность требует баланса между фокусом и расслаблением",
  "🌙 Сон помогает обрабатывать эмоции",
  "🎯 Цели дают направление и смысл",
  "📊 Эмоции помогают запоминать события",
  "🧠 Мозг оптимизирует энергопотребление",
  "💤 Качественный сон улучшает реакцию",
  "🎪 Перерывы предотвращают выгорание",
  "🌿 Природа восстанавливает внимание",
  "💧 Вода улучшает метаболизм",
  "🍫 Какао содержит магний, улучшающий сон",
  "🧬 Пластичность мозга позволяет адаптироваться",
  "🎨 Творчество развивает оригинальность мышления",
  "💭 Медитация улучшает качество жизни",
  "🏃 Физическая активность укрепляет кости",
  "📚 Чтение развивает критическое мышление",
  "🎵 Музыка может снижать боль",
  "🧘 Практика осознанности улучшает сон",
  "🤝 Социальная поддержка важна для выздоровления",
  "💡 Инсайты происходят при смене деятельности",
  "🌙 Регулярный сон улучшает метаболизм",
  "🎯 Цели помогают расставлять приоритеты",
  "📊 Эмоции влияют на иммунную систему",
  "🧠 Мозг создает модели реальности",
  "💤 Сон помогает восстанавливать тело",
  "🎪 Отдых улучшает креативность",
  "🌿 Природа снижает артериальное давление",
  "💧 Вода улучшает пищеварение",
  "🍫 Шоколад содержит антиоксиданты",
  "🧬 Нейрогенез стимулируется упражнениями",
  "🎨 Творчество развивает воображение",
  "💭 Осознанность улучшает отношения",
  "🏃 Упражнения повышают выносливость",
  "📚 Обучение расширяет кругозор",
  "🎵 Музыка может улучшать координацию",
  "🧘 Медитация снижает воспаление",
  "🤝 Социальные связи дают чувство принадлежности",
  "💡 Креативность требует разнообразия опыта",
  "🌙 Сон помогает расти и восстанавливаться",
  "🎯 Цели мотивируют к действию",
  "📊 Эмоции помогают ориентироваться в мире",
  "🧠 Мозг постоянно обновляет информацию",
  "💤 Качественный сон улучшает иммунитет",
  "🎪 Перерывы повышают мотивацию",
  "🌿 Природа улучшает настроение",
  "💧 Вода необходима для жизни",
  "🍫 Какао содержит железо",
  "🧬 Пластичность мозга — основа обучения",
  "🎨 Творчество развивает самовыражение",
  "💭 Медитация улучшает самооценку",
  "🏃 Физическая активность укрепляет сердце",
  "📚 Чтение развивает эмпатию",
  "🎵 Музыка может улучшать память",
  "🧘 Практика осознанности снижает стресс",
  "🤝 Социальная поддержка дает силы",
  "💡 Инсайты происходят при расслаблении",
  "🌙 Регулярный сон улучшает здоровье",
  "🎯 Цели помогают измерять прогресс",
  "📊 Эмоции делают жизнь ярче",
  "🧠 Мозг — самый сложный орган",
  "💤 Сон необходим для здоровья",
  "🎪 Отдых восстанавливает энергию",
  "🌿 Природа успокаивает ум",
  "💧 Вода улучшает кожу",
  "🍫 Шоколад поднимает настроение",
  "🧬 Нейрогенез происходит всю жизнь",
  "🎨 Творчество развивает гибкость",
  "💭 Осознанность улучшает фокус",
  "🏃 Упражнения укрепляют мышцы",
  "📚 Обучение обогащает жизнь",
  "🎵 Музыка объединяет людей",
  "🧘 Медитация успокаивает ум",
  "🤝 Социальные связи делают нас счастливее"
];

function LoadingMessage() {
  const [factIndex, setFactIndex] = useState(0);
  const [showFact, setShowFact] = useState(false);
  const [key, setKey] = useState(0); // Для принудительного ререндера при смене факта

  useEffect(() => {
    // Выбираем случайный факт при первом рендере
    setFactIndex(Math.floor(Math.random() * INTERESTING_FACTS.length));
    
    // Показываем основное сообщение, затем через 2 секунды начинаем показывать факты
    const factTimer = setTimeout(() => setShowFact(true), 2000);
    
    // Меняем факты каждые 30 секунд на случайный
    const interval = setInterval(() => {
      setFactIndex(() => {
        const randomIndex = Math.floor(Math.random() * INTERESTING_FACTS.length);
        setKey(prev => prev + 1); // Меняем key для анимации
        return randomIndex;
      });
    }, 30000);

    return () => {
      clearTimeout(factTimer);
      clearInterval(interval);
    };
  }, []);

  return (
    <div className="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl border-2 border-blue-200 p-6 shadow-sm">
      <div className="flex items-start gap-4">
        <div className="flex-shrink-0">
          <svg className="w-6 h-6 animate-spin text-blue-600" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"></path>
          </svg>
        </div>
        <div className="flex-grow">
          <div className="font-semibold text-gray-900 mb-2">
            Уже делаю расчёт, пожалуйста, подождите...
          </div>
          {showFact && (
            <div className="mt-3 p-3 bg-white rounded-xl border border-blue-100" key={key}>
              <div className="text-sm text-gray-700 animate-fade-in">
                {INTERESTING_FACTS[factIndex]}
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}

type Profile = {
  id: number;
  html?: string | null;
  raw_json?: any;
  created_at?: string;
  client_id?: number | null;
  ui_state?: any;
  notes?: string | null;
  chat_history?: Array<{ role: "user" | "assistant"; content: string }>;
  images?: any; // Может быть массивом файлов или строкой
  digits?: any;
};

export default function ProfileDetail() {
  const params = useParams();
  const id = params?.id as string;
  const [profile, setProfile] = useState<Profile | null>(null);
  const [clientName, setClientName] = useState<string>("");
  const [question, setQuestion] = useState("");
  const [answer, setAnswer] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);
  const [polling, setPolling] = useState(true);
  const pollingRef = useRef(true);
  const [navigationExpanded, setNavigationExpanded] = useState(true);
  const [expandAll, setExpandAll] = useState(false);
  const localUiStateRef = useRef<Record<string, boolean>>({});
  const [checkedMap, setCheckedMap] = useState<Record<string, boolean>>({});
  const savingCheckboxRef = useRef(false);
  const [notesOpen, setNotesOpen] = useState(false);
  const [notesDraft, setNotesDraft] = useState<string>("");
  const [savingNotes, setSavingNotes] = useState(false);
  const notesTouchedRef = useRef(false);
  const notesTextareaRef = useRef<HTMLTextAreaElement | null>(null);
  const notesOpenRef = useRef(false);
  useEffect(() => { notesOpenRef.current = notesOpen; }, [notesOpen]);
  const [chat, setChat] = useState<Array<{ role: "user" | "assistant"; content: string }>>([]);
  const chatRef = useRef<Array<{ role: "user" | "assistant"; content: string }>>([]);
  useEffect(() => { chatRef.current = chat; }, [chat]);
  const chatBoxRef = useRef<HTMLDivElement | null>(null);

  useEffect(() => {
    // авто-прокрутка вниз при появлении новых сообщений
    try {
      chatBoxRef.current?.scrollTo({ top: chatBoxRef.current.scrollHeight, behavior: "smooth" });
    } catch {}
  }, [chat]);

  function ActionBar() {
    async function exportPdf() {
      let strengths: string[] = [];
      let weaknesses: string[] = [];
      let personalitySummary: string = '';
      let happinessFormula: string = '';
      let digits: any[] = [];
      let resourceSignals: string[] = [];
      let deficitSignals: string[] = [];
      let checkedResource: Record<string, boolean> = {};
      let checkedDeficit: Record<string, boolean> = {};
      
      try {
        let payload: any = profile?.raw_json;
        if (typeof payload === 'string') payload = JSON.parse(payload);
        const item = Array.isArray(payload) ? payload[0] : payload;
        
        // Обрабатываем сильные стороны - убираем формат словаря
        if (item?.strengths) {
          if (Array.isArray(item.strengths)) {
            strengths = item.strengths.map((s: any) => {
              if (typeof s === 'string') return s;
              if (typeof s === 'object' && s !== null) {
                // Если это объект, берем значение или текст
                return s.text || s.value || s.content || JSON.stringify(s).replace(/[{}"]/g, '');
              }
              return String(s);
            }).filter(Boolean);
          } else if (typeof item.strengths === 'string') {
            strengths = item.strengths.split(/\n+/).filter(Boolean);
          } else if (item.strengths_text) {
            strengths = String(item.strengths_text).split(/\n+/).filter(Boolean);
          }
        }
        
        // Обрабатываем слабые стороны - убираем формат словаря
        if (item?.weaknesses) {
          if (Array.isArray(item.weaknesses)) {
            weaknesses = item.weaknesses.map((w: any) => {
              if (typeof w === 'string') return w;
              if (typeof w === 'object' && w !== null) {
                return w.text || w.value || w.content || JSON.stringify(w).replace(/[{}"]/g, '');
              }
              return String(w);
            }).filter(Boolean);
          } else if (typeof item.weaknesses === 'string') {
            weaknesses = item.weaknesses.split(/\n+/).filter(Boolean);
          } else if (item.weaknesses_text) {
            weaknesses = String(item.weaknesses_text).split(/\n+/).filter(Boolean);
          }
        }
        
        // Чеклисты ресурсов - убираем формат словаря
        if (item?.resourceSignals) {
          if (Array.isArray(item.resourceSignals)) {
            resourceSignals = item.resourceSignals.map((r: any) => {
              if (typeof r === 'string') return r;
              if (typeof r === 'object' && r !== null) {
                return r.text || r.value || r.content || JSON.stringify(r).replace(/[{}"]/g, '');
              }
              return String(r);
            }).filter(Boolean);
          } else if (typeof item.resourceSignals === 'string') {
            resourceSignals = item.resourceSignals.split(/\n+/).filter(Boolean);
          } else if (item.resourceSignals_text) {
            resourceSignals = String(item.resourceSignals_text).split(/\n+/).filter(Boolean);
          }
        }
        
        if (item?.deficitSignals) {
          if (Array.isArray(item.deficitSignals)) {
            deficitSignals = item.deficitSignals.map((d: any) => {
              if (typeof d === 'string') return d;
              if (typeof d === 'object' && d !== null) {
                return d.text || d.value || d.content || JSON.stringify(d).replace(/[{}"]/g, '');
              }
              return String(d);
            }).filter(Boolean);
          } else if (typeof item.deficitSignals === 'string') {
            deficitSignals = item.deficitSignals.split(/\n+/).filter(Boolean);
          } else if (item.deficitSignals_text) {
            deficitSignals = String(item.deficitSignals_text).split(/\n+/).filter(Boolean);
          }
        }
        
        // Получаем состояние чекбоксов из ui_state
        const uiState = (profile as any)?.ui_state;
        if (uiState?.checked) {
          checkedResource = Object.fromEntries(
            Object.entries(uiState.checked).filter(([key]) => key.includes('resourceSignals'))
          ) as Record<string, boolean>;
          checkedDeficit = Object.fromEntries(
            Object.entries(uiState.checked).filter(([key]) => key.includes('deficitSignals'))
          ) as Record<string, boolean>;
        }
        
        if (item?.personalitySummary) {
          personalitySummary = Array.isArray(item.personalitySummary) 
            ? item.personalitySummary.join('\n\n') 
            : String(item.personalitySummary);
        }
        
        if (item?.happinessFormula) {
          happinessFormula = Array.isArray(item.happinessFormula)
            ? item.happinessFormula.join('\n\n')
            : String(item.happinessFormula);
        }
        
        // Получаем digits
        const d = (profile as any)?.digits;
        if (Array.isArray(d)) digits = d;
        else if (typeof d === 'string') {
          try {
            const parsed = JSON.parse(d);
            digits = Array.isArray(parsed) ? parsed : String(d).split(/[,\s]+/).filter(Boolean);
          } catch {
            digits = String(d).split(/[,\s]+/).filter(Boolean);
          }
        }
      } catch {}
      
      let contact = "";
      let initials = "";
      try {
        const meRes = await fetch('/api/me', { cache: 'no-store' });
        const me = await meRes.json().catch(()=>({}));
        const user = me?.data || {};
        initials = [user.first_name, user.last_name].filter(Boolean).map((n:string)=>n?.[0]?.toUpperCase() || '').filter(Boolean).join('.');
        if (initials) initials += '.';
        contact = user.contact || '';
      } catch {}
      
      const dateStr = profile?.created_at ? new Date(profile.created_at).toLocaleDateString('ru-RU') : '';
      
      // Функция для очистки текста от формата словаря
      const cleanText = (text: string): string => {
        if (!text) return '';
        let cleaned = String(text);
        // Убираем формат {d:d} или подобные паттерны
        cleaned = cleaned.replace(/\{[^}]*\}/g, '');
        // Убираем лишние пробелы
        cleaned = cleaned.replace(/\s+/g, ' ').trim();
        return cleaned;
      };
      
      // Создаем HTML с красивым оформлением, один блок - одна страница
      const html = `
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <style>
    @page {
      size: A4;
      margin: 20mm;
    }
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      font-size: 14px;
      line-height: 1.7;
      color: #1a1a1a;
      background: white;
    }
    .page {
      page-break-after: always;
      page-break-inside: avoid;
      min-height: 250mm;
      padding: 20mm;
      display: flex;
      flex-direction: column;
      break-after: page;
    }
    .page:last-child {
      page-break-after: auto;
      break-after: auto;
    }
    .header {
      margin-bottom: 30px;
      padding-bottom: 15px;
      border-bottom: 3px solid #1f92aa;
    }
    .title {
      font-size: 28px;
      font-weight: 700;
      color: #1f92aa;
      margin-bottom: 8px;
    }
    .subtitle {
      font-size: 13px;
      color: #666;
    }
    .section-title {
      font-size: 20px;
      font-weight: 600;
      color: #1a1a1a;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 2px solid #e0e0e0;
    }
    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }
    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    li {
      margin: 12px 0;
      padding-left: 30px;
      position: relative;
      line-height: 1.8;
      font-size: 15px;
    }
    li::before {
      content: "•";
      position: absolute;
      left: 0;
      color: #1f92aa;
      font-weight: bold;
      font-size: 20px;
    }
    .checklist-item {
      padding-left: 35px;
    }
    .checkmark {
      position: absolute;
      left: 0;
      font-size: 18px;
    }
    .checklist-title {
      font-size: 16px;
      font-weight: 600;
      margin: 20px 0 12px 0;
      color: #059669;
    }
    .checklist-title.minus {
      color: #dc2626;
    }
    .footer {
      margin-top: auto;
      padding-top: 20px;
      border-top: 1px solid #e0e0e0;
      font-size: 11px;
      color: #666;
      text-align: right;
    }
  </style>
</head>
<body>
  ${strengths.length > 0 ? `
  <div class="page">
    <div class="header">
      <div class="title">Расчёт профиля${clientName ? ' — ' + clientName : ''}</div>
      ${dateStr ? `<div class="subtitle">Дата создания: ${dateStr}</div>` : ''}
    </div>
    <div class="content">
      <h2 class="section-title">Сильные стороны</h2>
      <ul>
        ${strengths.map((s: string) => {
          const cleaned = cleanText(s);
          return cleaned ? `<li>${cleaned.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</li>` : '';
        }).filter(Boolean).join('')}
      </ul>
    </div>
    <div class="footer">
      ${initials ? `<div>${initials}</div>` : ''}
      ${contact ? `<div>${contact}</div>` : ''}
    </div>
  </div>
  ` : ''}
  
  ${weaknesses.length > 0 ? `
  <div class="page">
    <div class="header">
      <div class="title">Расчёт профиля${clientName ? ' — ' + clientName : ''}</div>
      ${dateStr ? `<div class="subtitle">Дата создания: ${dateStr}</div>` : ''}
    </div>
    <div class="content">
      <h2 class="section-title">Слабые стороны</h2>
      <ul>
        ${weaknesses.map((w: string) => {
          const cleaned = cleanText(w);
          return cleaned ? `<li>${cleaned.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</li>` : '';
        }).filter(Boolean).join('')}
      </ul>
    </div>
    <div class="footer">
      ${initials ? `<div>${initials}</div>` : ''}
      ${contact ? `<div>${contact}</div>` : ''}
    </div>
  </div>
  ` : ''}
  
  ${(resourceSignals.length > 0 || deficitSignals.length > 0) ? `
  <div class="page">
    <div class="header">
      <div class="title">Расчёт профиля${clientName ? ' — ' + clientName : ''}</div>
      ${dateStr ? `<div class="subtitle">Дата создания: ${dateStr}</div>` : ''}
    </div>
    <div class="content">
      <h2 class="section-title">Чек-лист ресурсов</h2>
      ${resourceSignals.length > 0 ? `
      <div>
        <div class="checklist-title">Признаки плюса (ресурс)</div>
        <ul>
          ${resourceSignals.map((r: string, idx: number) => {
            const cleaned = cleanText(r);
            if (!cleaned) return '';
            const isChecked = Object.keys(checkedResource).some(k => 
              k.includes('resourceSignals') && (k.includes(String(idx)) || k.includes(r.slice(0, 20)))
            );
            const checkmark = isChecked ? '☑' : '☐';
            return `<li class="checklist-item"><span class="checkmark">${checkmark}</span>${cleaned.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</li>`;
          }).filter(Boolean).join('')}
        </ul>
      </div>
      ` : ''}
      ${deficitSignals.length > 0 ? `
      <div>
        <div class="checklist-title minus">Признаки минуса (дефицит)</div>
        <ul>
          ${deficitSignals.map((d: string, idx: number) => {
            const cleaned = cleanText(d);
            if (!cleaned) return '';
            const isChecked = Object.keys(checkedDeficit).some(k => 
              k.includes('deficitSignals') && (k.includes(String(idx)) || k.includes(d.slice(0, 20)))
            );
            const checkmark = isChecked ? '☑' : '☐';
            return `<li class="checklist-item"><span class="checkmark">${checkmark}</span>${cleaned.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</li>`;
          }).filter(Boolean).join('')}
        </ul>
      </div>
      ` : ''}
    </div>
    <div class="footer">
      ${initials ? `<div>${initials}</div>` : ''}
      ${contact ? `<div>${contact}</div>` : ''}
    </div>
  </div>
  ` : ''}
</body>
</html>`;
      
      // Создаем временный элемент для генерации PDF
      // Используем DOMParser для правильного парсинга HTML
      const parser = new DOMParser();
      const doc = parser.parseFromString(html, 'text/html');
      
      // Проверяем ошибки парсинга
      const parserError = doc.querySelector('parsererror');
      if (parserError) {
        console.error('HTML parsing error:', parserError.textContent);
        alert('Ошибка парсинга HTML: ' + parserError.textContent);
        return;
      }
      
      const bodyContent = doc.body;
      const styleElement = doc.querySelector('style');
      
      console.log('PDF generation debug:', {
        hasDoc: !!doc,
        hasBody: !!bodyContent,
        bodyChildren: bodyContent?.children.length || 0,
        hasStyle: !!styleElement,
        htmlLength: html.length
      });
      
      if (!bodyContent || bodyContent.children.length === 0) {
        console.error('Body content is empty or missing', {
          bodyContent,
          bodyHTML: bodyContent?.innerHTML,
          docHTML: doc.documentElement?.outerHTML?.substring(0, 500)
        });
        alert('Ошибка: не удалось создать содержимое для PDF. Проверьте консоль для деталей.');
        return;
      }
      
      // Создаем контейнер для PDF
      const element = document.createElement('div');
      // Делаем элемент видимым для html2canvas, но вне видимой области экрана
      element.style.width = '210mm';
      element.style.minHeight = '297mm';
      element.style.padding = '0';
      element.style.margin = '0';
      element.style.backgroundColor = 'white';
      element.style.position = 'absolute';
      element.style.top = '-9999px'; // Сдвигаем за экран, но элемент остается в DOM
      element.style.left = '0';
      element.style.visibility = 'visible';
      element.style.opacity = '1';
      element.style.pointerEvents = 'none';
      element.style.overflow = 'visible';
      element.style.zIndex = '999999';
      
      // Копируем стили
      if (styleElement) {
        const style = document.createElement('style');
        style.textContent = styleElement.textContent || '';
        element.appendChild(style);
      }
      
      // Копируем содержимое body
      const bodyClone = bodyContent.cloneNode(true) as HTMLElement;
      while (bodyClone.firstChild) {
        element.appendChild(bodyClone.firstChild);
      }
      
      document.body.appendChild(element);
      
      // Ждем, чтобы стили применились и контент отрендерился
      // Увеличиваем задержку для полного рендеринга
      await new Promise(resolve => setTimeout(resolve, 500));
      
      // Дополнительная проверка - убеждаемся, что контент отрендерился
      const textContent = element.textContent?.trim() || '';
      const hasContent = element.children.length > 0 || textContent.length > 0;
      console.log('PDF element ready:', {
        childrenCount: element.children.length,
        hasText: textContent.length > 0,
        computedStyle: window.getComputedStyle(element).display
      });
      
      if (!hasContent) {
        console.error('PDF element has no content after rendering');
        document.body.removeChild(element);
        alert('Ошибка: контент не отрендерился для PDF');
        return;
      }
      
      // Настройки для html2pdf
      const opt = {
        margin: [0, 0, 0, 0] as [number, number, number, number],
        filename: `Расчет_${clientName || 'профиль'}_${dateStr || new Date().toLocaleDateString('ru-RU')}.pdf`,
        image: { type: 'jpeg' as const, quality: 0.98 },
        html2canvas: { 
          scale: 2, 
          useCORS: true,
          logging: false,
          backgroundColor: '#ffffff',
          width: element.scrollWidth || 794, // A4 width in pixels at 96 DPI
          height: element.scrollHeight || 1123, // A4 height in pixels
          windowWidth: element.scrollWidth || 794,
          windowHeight: element.scrollHeight || 1123,
          removeContainer: false
        },
        jsPDF: { unit: 'mm' as const, format: 'a4' as const, orientation: 'portrait' as const },
        pagebreak: { mode: ['avoid-all', 'css', 'legacy'] }
      };
      
      // Генерируем и скачиваем PDF
      try {
        await html2pdf().set(opt).from(element).save();
      } catch (error) {
        console.error('Error generating PDF:', error);
        alert('Ошибка при генерации PDF: ' + (error instanceof Error ? error.message : String(error)));
      } finally {
        if (element.parentNode) {
          document.body.removeChild(element);
        }
      }
    }
    // TODO: Доработать функционал экспорта PDF позже
    // Показываем PDF только для базовых расчетов
    // if (consultationType !== "base") {
    //   return null;
    // }
    
    // return (
    //   <div className="flex items-center gap-2 text-sm flex-shrink-0">
    //     <button onClick={exportPdf} className="px-3 py-1.5 rounded-lg border hover:bg-gray-50">📄 PDF</button>
    //   </div>
    // );
    
    return null;
  }

  function AccordionSection({ title, children, id }: { title: string; children: React.ReactNode; id?: string }) {
    // Всегда открыто, без кнопки, чтобы ничего не закрывалось само
    return (
      <div id={id} className="rounded-2xl border-2 border-blue-200 bg-white overflow-hidden shadow-sm">
        <div className="w-full flex items-center justify-between px-6 py-4 text-left bg-white">
          <span className="font-semibold text-gray-900">{title}</span>
        </div>
        <div className="p-6">{children}</div>
      </div>
    );
  }

  // Первичная загрузка и поллинг до появления html/данных
  useEffect(() => {
    let mounted = true;
    let tries = 0;
    const maxTries = 90; // до ~3 минут

    async function fetchOnce() {
      let data: any = null;
      let responseStatus: number | null = null;
      try {
        const res = await fetch(`/api/profiles/${id}?_=${Date.now()}`, {
          cache: "no-store",
          credentials: "include",
          headers: { Accept: "application/json" },
          mode: "same-origin",
        } as RequestInit);
        responseStatus = res.status;
        if (!res.ok) {
          // 401/403/5xx — мягко завершаем попытку
          try { data = await res.json(); } catch { data = null; }
        } else {
          data = await res.json();
        }
      } catch {
        // network/CORS — не роняем, попробуем позже
        data = null;
      }
      if (!mounted) return;
      const p = data?.data || null;
      
      // Логируем данные для диагностики
      console.log("API response:", {
        status: responseStatus,
        hasData: !!data,
        hasDataData: !!data?.data,
        profileKeys: p ? Object.keys(p) : [],
        fullResponse: data
      });
      
      if (p) {
        console.log("Profile data received:", {
          id: p.id,
          hasHtml: !!(p.html || p.raw_html || p.content || p.html_content),
          htmlValue: p.html ? String(p.html).substring(0, 100) : null,
          hasRawJson: !!p.raw_json,
          rawJsonType: typeof p.raw_json,
          rawJsonLength: p.raw_json ? (typeof p.raw_json === 'string' ? p.raw_json.length : JSON.stringify(p.raw_json).length) : 0,
          rawJsonPreview: p.raw_json ? (typeof p.raw_json === 'string' ? p.raw_json.substring(0, 100) : JSON.stringify(p.raw_json).substring(0, 100)) : null,
          hasDigits: !!p.digits,
          digitsType: typeof p.digits,
          digitsValue: p.digits
        });
      } else {
        console.warn("⚠️ Profile data is null!", { data, status: responseStatus });
      }
      
      // если идёт локальное сохранение чекбоксов — не перетирать ui_state
      setProfile(prev => {
        if (!prev) return p;
        if (savingCheckboxRef.current && prev?.ui_state?.checked) {
          return { ...p, ui_state: prev.ui_state };
        }
        return p;
      });
      // Инициализация чекбоксов из ui_state + слияние с локальными изменениями
      const serverChecked: Record<string, boolean> = (p?.ui_state?.checked as any) || {};
      const merged = { ...serverChecked, ...localUiStateRef.current };
      setCheckedMap(merged);
      // Инициализация заметок: не перетираем, если пользователь редактирует сейчас
      if (!notesTouchedRef.current && !notesOpenRef.current) {
        setNotesDraft(p?.notes || "");
      }
      if (Array.isArray(p?.chat_history) && chatRef.current.length === 0) {
        setChat(p.chat_history);
      }
      // Подтянем имя клиента
      if (p?.client_id) {
        try {
          const rc = await fetch(`/api/clients/${p.client_id}`, { cache: "no-store" });
          const cj = await rc.json().catch(()=>({}));
          const name = cj?.data?.name || "";
          if (name) setClientName(name);
        } catch {}
      }
      const htmlCandidate = p?.html || p?.raw_html || p?.content || p?.html_content;
      const hasRenderableHtml = Boolean(htmlCandidate && String(htmlCandidate).trim().length > 0);
      
      // Проверяем raw_json более тщательно
      let hasRaw = false;
      if (p?.raw_json) {
        try {
          // raw_json может быть строкой или объектом
          let rawStr: string;
          if (typeof p.raw_json === 'string') {
            rawStr = p.raw_json.trim();
            // Если это JSON-строка, попробуем распарсить для проверки
            try {
              const parsed = JSON.parse(rawStr);
              // Если распарсилось, проверяем что это не пустой объект/массив
              if (typeof parsed === 'object' && parsed !== null) {
                hasRaw = Object.keys(parsed).length > 0 || Array.isArray(parsed) && parsed.length > 0;
              } else {
                hasRaw = rawStr.length > 2 && rawStr !== 'null' && rawStr !== 'undefined';
              }
            } catch {
              // Не JSON, просто строка - проверяем длину
              hasRaw = rawStr.length > 2;
            }
          } else {
            // Это уже объект
            rawStr = JSON.stringify(p.raw_json);
            hasRaw = rawStr.length > 2 && rawStr !== '{}' && rawStr !== '[]' && rawStr !== 'null';
          }
        } catch {
          hasRaw = false;
        }
      }
      
      // Также проверяем наличие digits (могут быть данные даже без html)
      const hasDigits = Boolean(p?.digits && (
        (Array.isArray(p.digits) && p.digits.length > 0) ||
        (typeof p.digits === 'string' && p.digits.trim().length > 0)
      ));
      
      tries += 1;
          
          // Останавливаем поллинг если:
          // 1. Есть данные для отображения
          // 2. Достигнут лимит попыток
          // 3. Получили 401 и refresh не помог (нужен перелогин)
          const isUnauthorized = responseStatus === 401 && !p;
          const shouldStop = hasRenderableHtml || hasRaw || hasDigits || tries >= maxTries || isUnauthorized;
      
      if (shouldStop) {
        if (pollingRef.current) {
          setPolling(false);
          pollingRef.current = false;
          console.log("✅ Polling stopped:", { 
            hasRenderableHtml, 
            hasRaw, 
            hasDigits, 
            tries,
            profileId: p?.id,
            htmlLength: htmlCandidate ? String(htmlCandidate).length : 0
          });
        }
      } else {
        if (pollingRef.current) {
          console.log("⏳ Still polling:", { 
            hasRenderableHtml, 
            hasRaw, 
            hasDigits, 
            tries, 
            profileId: p?.id,
            maxTries,
            htmlCandidate: htmlCandidate ? String(htmlCandidate).substring(0, 50) : null
          });
        }
      }
    }

    // моментальный первый запрос
    fetchOnce();
    // далее поллинг только если еще не остановлен
    const interval = setInterval(() => {
      if (pollingRef.current) {
        fetchOnce();
      } else {
        clearInterval(interval);
      }
    }, 1800);

    return () => {
      mounted = false;
      clearInterval(interval);
    };
  }, [id]);

  async function ask(e: React.FormEvent) {
    e.preventDefault();
    const q = question.trim();
    if (!q) return;
    setLoading(true);
    setAnswer("");
    // добавим пользователя в историю
    const history: Array<{ role: "user" | "assistant"; content: string }> = [...chatRef.current, { role: "user" as const, content: q }];
    setChat(history);
    setQuestion("");
    try {
      const res = await fetch(`/api/qa?stream=1`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Accept: "text/event-stream", "Cache-Control": "no-cache" },
        body: JSON.stringify({
          profileId: Number(id),
          question: q,
          history: chatRef.current.slice(-8),
          stream: true,
        }),
      } as RequestInit);

      // Если сервер не отдал поток — fallback на обычный json
      if (!res.body || (res.headers.get("content-type") || "").indexOf("text/event-stream") === -1) {
        let a = "Нет ответа";
        let errorMessage = "";
        try {
          const data = await res.json();
          a = data?.answer || data?.message || a;
          errorMessage = data?.error || "";
          
          // Если это ошибка, показываем более понятное сообщение
          if (!res.ok) {
            if (res.status === 401) {
              a = "Ошибка: Неверный API ключ OpenAI. Проверьте настройки сервера.";
            } else if (res.status === 429) {
              a = "Ошибка: Превышен лимит запросов к OpenAI API. Попробуйте позже.";
            } else if (data?.message) {
              a = `Ошибка: ${data.message}`;
            } else {
              a = `Ошибка: ${res.status} ${res.statusText}`;
            }
          }
        } catch {
          a = `Ошибка: ${res.status} ${res.statusText || "Неизвестная ошибка"}`;
        }
        const newHistory: Array<{ role: "user" | "assistant"; content: string }> = [...history, { role: "assistant" as const, content: a }];
        setAnswer(a);
        setChat(newHistory);
        await saveChatHistory(newHistory);
        setLoading(false);
        return;
      }

      const reader = res.body.getReader();
      const decoder = new TextDecoder();
      let acc = "";
      // добавим пустое сообщение ассистента, будем дописывать
      setChat((prev)=>[...prev, { role: "assistant", content: "" }]);
      
      try {
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
          
        const chunk = decoder.decode(value, { stream: true });
          // SSE формат: каждая строка "data: текст\n\n"
          const lines = chunk.split("\n");
          
          for (const line of lines) {
            if (!line.trim()) continue;
            
            // Пропускаем не-SSE строки
            if (!line.startsWith("data:")) continue;
            
            const payload = line.slice(5).trim(); // Убираем "data:"
            
            if (payload === "[DONE]") {
              break;
            }
            
            // Payload может быть JSON строкой или обычным текстом
            // Пытаемся распарсить как JSON, если не получается - используем как есть
            let textChunk = payload;
            try {
              const parsed = JSON.parse(payload);
              if (typeof parsed === 'string') {
                textChunk = parsed;
              }
            } catch {
              // Если не JSON, используем как есть (для обратной совместимости)
              textChunk = payload;
            }
            acc += textChunk;
          setAnswer(acc);
            
            // Обновляем последнее сообщение ассистента
          setChat((prev)=>{
              const next = [...prev];
            for (let i = next.length - 1; i >= 0; i--) {
                if (next[i].role === "assistant") {
                  next[i] = { role: "assistant", content: acc };
                  break;
                }
            }
            return next;
          });
            
            // Автопрокрутка чата
            if (chatBoxRef.current) {
              chatBoxRef.current.scrollTop = chatBoxRef.current.scrollHeight;
        }
      }
        }
      } catch (streamError) {
        console.error("Stream reading error:", streamError);
        if (acc) {
          // Если уже есть текст, сохраняем его
      const finalHistory: Array<{ role: "user" | "assistant"; content: string }> = [...history, { role: "assistant" as const, content: acc }];
          setChat(finalHistory);
      await saveChatHistory(finalHistory);
        }
      }
      const finalHistory: Array<{ role: "user" | "assistant"; content: string }> = [...history, { role: "assistant" as const, content: acc }];
      await saveChatHistory(finalHistory);
    } catch (err: any) {
      console.error("Chat error:", err);
      const errorMsg = err?.message || "Ошибка чата";
      const errorHistory: Array<{ role: "user" | "assistant"; content: string }> = [...history, { role: "assistant" as const, content: `Ошибка: ${errorMsg}` }];
      setAnswer(`Ошибка: ${errorMsg}`);
      setChat(errorHistory);
      await saveChatHistory(errorHistory);
    } finally {
      setLoading(false);
    }
  }

  function wrapSelection(prefix: string, suffix = prefix) {
    const ta = notesTextareaRef.current;
    if (!ta) return;
    const { selectionStart, selectionEnd } = ta;
    const before = notesDraft.slice(0, selectionStart);
    const selected = notesDraft.slice(selectionStart, selectionEnd);
    const after = notesDraft.slice(selectionEnd);
    const next = before + prefix + selected + suffix + after;
    setNotesDraft(next);
    notesTouchedRef.current = true;
    const cursorStart = selectionStart + prefix.length;
    const cursorEnd = cursorStart + selected.length;
    requestAnimationFrame(() => {
      ta.focus();
      ta.setSelectionRange(cursorStart, cursorEnd);
    });
  }

  function prefixLines(prefix: string) {
    const ta = notesTextareaRef.current;
    if (!ta) return;
    let { selectionStart, selectionEnd } = ta;
    if (selectionStart === selectionEnd) {
      selectionStart = notesDraft.lastIndexOf("\n", selectionStart - 1) + 1;
      const lineEnd = notesDraft.indexOf("\n", selectionEnd);
      selectionEnd = lineEnd === -1 ? notesDraft.length : lineEnd;
    }
    const before = notesDraft.slice(0, selectionStart);
    const selected = notesDraft.slice(selectionStart, selectionEnd);
    const after = notesDraft.slice(selectionEnd);
    const newSelected = selected.split("\n").map(line => prefix + line).join("\n");
    const next = before + newSelected + after;
    setNotesDraft(next);
    notesTouchedRef.current = true;
    requestAnimationFrame(() => {
      ta.focus();
      ta.setSelectionRange(selectionStart, selectionStart + newSelected.length);
    });
  }

  // Сохранение состояния чекбоксов в Directus (profiles.ui_state JSON)
  const saveChecked = useCallback(async (nextMap: Record<string, boolean>) => {
    // Очередь сохранений, чтобы клик не терялся при поллинге
    (saveChecked as any)._queue = (saveChecked as any)._queue || [];
    (saveChecked as any)._queue.push(nextMap);
    if ((saveChecked as any)._running) return;
    (saveChecked as any)._running = true;
    try {
      savingCheckboxRef.current = true;
      while ((saveChecked as any)._queue.length) {
        const payload = (saveChecked as any)._queue.pop();
        localUiStateRef.current = payload;
        const res = await fetch(`/api/profiles/${id}`, {
          method: "PATCH",
          headers: { "Content-Type": "application/json" },
          credentials: "include",
          cache: "no-store",
          body: JSON.stringify({ ui_state: { checked: payload } }),
        });
        if (res.ok) setProfile((p) => (p ? { ...p, ui_state: { checked: payload } } : p));
      }
    } finally {
      savingCheckboxRef.current = false;
      (saveChecked as any)._running = false;
    }
  }, [id]);
  // Persist chat history in Directus
  const saveChatHistory = useCallback(async (history: Array<{ role: "user" | "assistant"; content: string }>) => {
    try {
      await fetch(`/api/profiles/${id}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ chat_history: history }),
      });
    } catch {}
  }, [id]);

  // Ключи чекбоксов: новый (по хэшу текста) + легаси (по индексу и усечённому тексту)
  function normalizeTextForHash(text: string): string {
    return (text || "").toString().replace(/\s+/g, " ").trim();
  }
  function hashString(input: string): string {
    // простая FNV-1a 32-bit
    let h = 0x811c9dc5;
    for (let i = 0; i < input.length; i++) {
      h ^= input.charCodeAt(i);
      h += (h << 1) + (h << 4) + (h << 7) + (h << 8) + (h << 24);
    }
    return (h >>> 0).toString(16);
  }

  // Определение типа консультации
  const consultationType = useMemo(() => {
    if (!profile?.raw_json) return null;
    let payload: any = profile.raw_json;
    try {
      if (typeof payload === "string") payload = JSON.parse(payload);
    } catch {
      return null;
    }
    const items = Array.isArray(payload) ? payload : (payload && typeof payload === 'object' ? [payload] : []);
    if (items.length === 0) return null;
    const item = items[0];
    
    // Партнерская консультация - есть compatibility или firstParticipant/secondParticipant
    if (item.compatibility || item.firstParticipantCodes || item.secondParticipantCodes || 
        item.partnerCodes || 
        (item.currentDiagnostics && (item.currentDiagnostics.firstParticipant || item.currentDiagnostics.secondParticipant))) {
      return "partner";
    }
    
    // Целевая консультация - есть goalDecomposition, warnings, plan123 и т.д., но нет opener/personalitySummary
    if ((item.goalDecomposition || item.warnings || item.plan123 || item.request) && 
        !item.opener && !item.personalitySummary) {
      return "target";
    }
    
    // Базовый расчет - есть opener, personalitySummary, strengths, weaknesses и т.д.
    if (item.opener || item.personalitySummary || item.strengths || item.weaknesses) {
      return "base";
    }
    
    return null;
  }, [profile?.raw_json]);

  const renderedFromJson = useMemo(() => {
    if (!profile?.raw_json) {
      console.log("[DEBUG] renderedFromJson: no raw_json");
      return null;
    }
    let payload: any = profile.raw_json;
    try {
      if (typeof payload === "string") payload = JSON.parse(payload);
    } catch (parseError) {
      console.warn("[DEBUG] renderedFromJson: failed to parse JSON", parseError);
      return null;
    }
    // Если payload - это объект (не массив), оборачиваем в массив
    // Но если это объект с полями целевой консультации напрямую, тоже обрабатываем
    let items: any[];
    if (Array.isArray(payload)) {
      items = payload;
    } else if (payload && typeof payload === 'object') {
      // Если это объект, проверяем - это один элемент или объект с полями целевой консультации
      // Если есть поля целевой консультации напрямую, используем как один элемент
      items = [payload];
    } else {
      items = [];
    }
    
    console.log("[DEBUG] renderedFromJson: parsed items", {
      itemsCount: items.length,
      firstItemKeys: items[0] ? Object.keys(items[0]) : [],
      hasOpener: items[0]?.opener,
      hasPersonalitySummary: items[0]?.personalitySummary,
      hasStrengths: items[0]?.strengths,
      hasWeaknesses: items[0]?.weaknesses,
      hasHappinessFormula: items[0]?.happinessFormula,
      hasCodesExplanation: items[0]?.codesExplanation,
      hasResourceSignals: items[0]?.resourceSignals,
      hasDeficitSignals: items[0]?.deficitSignals,
      hasConflicts: items[0]?.conflicts,
      hasPractices: items[0]?.practices,
      // Поля целевой консультации
      hasWarnings: Array.isArray(items[0]?.warnings),
      hasGoalDecomposition: Array.isArray(items[0]?.goalDecomposition),
      hasResourcesForStages: Array.isArray(items[0]?.resourcesForStages),
      hasCurrentDiagnostics: !!items[0]?.currentDiagnostics,
      hasPlan123: Array.isArray(items[0]?.plan123),
      hasProgressMetrics: !!items[0]?.progressMetrics,
      hasWhatIf: !!items[0]?.whatIf,
      hasObjectionHandling: Array.isArray(items[0]?.objectionHandling),
      hasFinalStrategy: Array.isArray(items[0]?.finalStrategy),
    });

    const makeHashKey = (section: string, text: string): string => `${section}:h:${hashString(normalizeTextForHash(text))}`;
    const makeLegacyKey = (section: string, text: string, index: number): string => {
      const norm = (text || "").toString().slice(0, 80);
      return `${section}:${index}:${norm}`;
    };

    const CheckList = ({ list, section }: { list: string[]; section: string }) => (
      <ul className="space-y-2">
        {list.map((text, i) => {
          const legacyKey = makeLegacyKey(section, text, i);
          const hashKey = makeHashKey(section, text);
          const checked = Boolean(checkedMap[hashKey] ?? checkedMap[legacyKey]);
          const inputId = `${hashKey}-${i}`;
          return (
            <li key={hashKey} className="grid grid-cols-[22px_1fr] gap-3 items-start">
              <input
                id={inputId}
                type="checkbox"
                checked={checked}
                onChange={(e)=>{
                  const next = { ...checkedMap, [hashKey]: e.target.checked, [legacyKey]: e.target.checked };
                  setCheckedMap(next);
                  localUiStateRef.current = next;
                  saveChecked(next);
                }}
                className="mt-0.5 h-[18px] w-[18px] rounded border-2 border-indigo-300 bg-indigo-100 text-indigo-600 focus:ring-0"
              />
              <label htmlFor={inputId} className="leading-relaxed text-gray-800 cursor-pointer">
                {text}
              </label>
            </li>
          );
        })}
      </ul>
    );

    const Entry = ({ title, children, color = "teal" as "teal" | "amber" }: { title?: string; children?: React.ReactNode; color?: "teal" | "amber" }) => (
      <div className="relative rounded-xl border-2 border-blue-100 p-4 pl-8">
        <span className={`absolute left-3 top-3 bottom-3 w-[6px] rounded-[3px] ${color === "teal" ? "bg-teal-500" : "bg-amber-500"}`} />
        {title && <h3 className="m-0 text-[18px] font-semibold">{title}</h3>}
        <div className="mt-2 space-y-2 text-gray-700">{children}</div>
      </div>
    );

    // Проверяем, есть ли хотя бы какие-то данные для отображения
    const hasAnyData = items.some((item: any) => {
      if (!item || typeof item !== 'object') return false;
      return !!(
        item.opener || 
        item.personalitySummary || 
        item.strengths || 
        item.strengths_text || 
        item.weaknesses || 
        item.weaknesses_text || 
        item.happinessFormula || 
        item.codesExplanation || 
        item.resourceSignals || 
        item.resourceSignals_text || 
        item.deficitSignals || 
        item.deficitSignals_text || 
        item.conflicts || 
        item.practices ||
        // Для целевых консультаций могут быть другие поля
        item.request ||
        item.answer ||
        item.recommendations ||
        item.analysis ||
        (Array.isArray(item.warnings) && item.warnings.length > 0) ||
        (Array.isArray(item.goalDecomposition) && item.goalDecomposition.length > 0) ||
        (Array.isArray(item.resourcesForStages) && item.resourcesForStages.length > 0) ||
        item.currentDiagnostics ||
        (Array.isArray(item.plan123) && item.plan123.length > 0) ||
        item.progressMetrics ||
        item.whatIf ||
        (Array.isArray(item.objectionHandling) && item.objectionHandling.length > 0) ||
        (Array.isArray(item.finalStrategy) && item.finalStrategy.length > 0) ||
        // Поля партнерской консультации
        item.compatibility ||
        item.firstParticipantCodes ||
        item.secondParticipantCodes ||
        item.partnerCodes ||
        (item.currentDiagnostics && (item.currentDiagnostics.firstParticipant || item.currentDiagnostics.secondParticipant || item.currentDiagnostics.conflictZones))
      );
    });
    
    console.log("[DEBUG] renderedFromJson: hasAnyData check", {
      hasAnyData,
      itemsLength: items.length,
      firstItemType: items[0] ? typeof items[0] : 'none'
    });
    
    if (!hasAnyData) {
      console.warn("[DEBUG] renderedFromJson: no recognizable data fields in items", {
        items,
        firstItemKeys: items[0] ? Object.keys(items[0]) : []
      });
      return null;
    }
    
    // Определяем тип консультации для первого элемента
    const item = items[0];
    const isPartner = !!(item?.compatibility || item?.firstParticipantCodes || item?.secondParticipantCodes || 
      item?.partnerCodes || 
      (item?.currentDiagnostics && (item.currentDiagnostics.firstParticipant || item.currentDiagnostics.secondParticipant)));
    const isTarget = !!(item?.goalDecomposition || item?.warnings || item?.plan123 || item?.request) && 
      !item?.opener && !item?.personalitySummary;
    const isBase = !!(item?.opener || item?.personalitySummary || item?.strengths || item?.weaknesses);

    // Выбираем функцию рендеринга в зависимости от типа
    if (isPartner) {
      return renderPartnerConsultation(items, CheckList, Entry, checkedMap, setCheckedMap, localUiStateRef, saveChecked);
    } else if (isTarget) {
      return renderTargetConsultation(items, CheckList, Entry, checkedMap, setCheckedMap, localUiStateRef, saveChecked);
    } else if (isBase) {
      return renderBaseConsultation(items, CheckList, Entry, checkedMap, setCheckedMap, localUiStateRef, saveChecked);
    }

    return null;
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [profile?.raw_json, checkedMap, saveChecked]);

  // Функция рендеринга базового расчета
  function renderBaseConsultation(
    items: any[],
    CheckList: any,
    Entry: any,
    checkedMap: Record<string, boolean>,
    setCheckedMap: (map: Record<string, boolean>) => void,
    localUiStateRef: React.MutableRefObject<Record<string, boolean>>,
    saveChecked: (map: Record<string, boolean>) => void
  ) {
    return (
      <div className="space-y-6">
        {items.map((item, idx) => (
          <div key={idx} className="space-y-6">
            {/* Скажите клиенту */}
                {item.opener && (
              <section id="opener" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 flex items-center gap-2 text-sm font-bold text-gray-800 mb-3">
                  <span className="text-lg">❗</span>
                      Скажите клиенту
                    </h2>
                    <p className="mt-3 whitespace-pre-wrap leading-relaxed text-gray-800">{item.opener}</p>
              </section>
                )}

            {/* Описание личности */}
                {item.personalitySummary && (
              <section id="personality" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">👤 Описание личности</h2>
                    {Array.isArray(item.personalitySummary) ? (
                      <div className="mt-3 space-y-2">
                        {item.personalitySummary.map((t: string, idx: number) => (
                          <p key={idx} className="whitespace-pre-wrap leading-relaxed text-gray-800">{t}</p>
                        ))}
                      </div>
                    ) : (
                      <p className="mt-3 whitespace-pre-wrap leading-relaxed text-gray-800">{item.personalitySummary}</p>
                )}
              </section>
            )}

            {/* Сильные и слабые стороны */}
            {(item.strengths || item.strengths_text || item.weaknesses || item.weaknesses_text) && (
              <section id="strengths-weaknesses" className="space-y-6">
                <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
                  <article className="rounded-2xl border-2 border-orange-300 bg-orange-50 p-6 shadow-sm">
                    <h3 className="m-0 text-base font-bold text-gray-900 mb-3">⚖️ Сильные стороны</h3>
                  <div className="mt-3">
                    {Array.isArray(item.strengths) ? (
                      <CheckList list={item.strengths} section="strengths" />
                    ) : (
                      item.strengths_text && (
                        <pre className="whitespace-pre-wrap text-sm leading-relaxed text-gray-900">{item.strengths_text}</pre>
                      )
                    )}
                  </div>
                </article>
                  <article className="rounded-2xl border-2 border-orange-300 bg-orange-50 p-6 shadow-sm">
                    <h3 className="m-0 text-base font-bold text-gray-900 mb-3">⚖️ Слабые стороны</h3>
                  <div className="mt-3">
                    {Array.isArray(item.weaknesses) ? (
                      <CheckList list={item.weaknesses} section="weaknesses" />
                    ) : (
                      item.weaknesses_text && (
                        <pre className="whitespace-pre-wrap text-sm leading-relaxed text-gray-900">{item.weaknesses_text}</pre>
                      )
                    )}
                  </div>
                </article>
                </div>
              </section>
            )}

            {/* Формула счастья */}
            {item.happinessFormula && (
              <section id="happiness" className="rounded-2xl border-2 border-sky-300 bg-sky-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">☺️ Формула счастья</h2>
                {Array.isArray(item.happinessFormula) ? (
                  <div className="mt-3 space-y-2">
                    {item.happinessFormula.map((t: string, i: number) => (
                      <p key={i} className="whitespace-pre-wrap leading-relaxed text-gray-800">{t}</p>
                    ))}
                  </div>
                ) : (
                  <p className="mt-3 whitespace-pre-wrap leading-relaxed text-gray-800">{item.happinessFormula}</p>
                )}
              </section>
            )}

            {/* Пояснение кодов */}
            {Array.isArray(item.codesExplanation) && item.codesExplanation.length > 0 && (
              <section id="codes" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">🔢 Пояснение кодов</h2>
                <div className="mt-3 space-y-2">
                  {item.codesExplanation.map((t: string, i: number) => (
                    <p key={i} className="whitespace-pre-wrap text-gray-800">{t}</p>
                  ))}
                </div>
              </section>
            )}

            {/* Диагностика ресурсов */}
            {(item.resourceSignals || item.resourceSignals_text || item.deficitSignals || item.deficitSignals_text) && (
              <section id="diagnostics" className="space-y-6">
                <article className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                  <h2 className="m-0 text-base font-bold text-gray-800 mb-3">🔍 Диагностика ресурсов</h2>
                  <p className="mt-3 text-sm text-gray-700">Оцените совпадение пунктов «плюса» и «минуса», подведите итог и сделайте вывод о текущем ресурсе.</p>
                </article>
                <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
                  <article className="rounded-2xl border-2 border-green-300 bg-green-50 p-6 shadow-sm">
                    <h3 className="m-0 text-base font-bold text-gray-900 mb-3">✅ Признаки плюса</h3>
                    <div className="mt-3">
                      {Array.isArray(item.resourceSignals) ? (
                        <CheckList list={item.resourceSignals} section="resourceSignals" />
                      ) : (
                        item.resourceSignals_text && (
                          <pre className="whitespace-pre-wrap text-sm leading-relaxed text-gray-900">{item.resourceSignals_text}</pre>
                        )
                      )}
                    </div>
                  </article>
                  <article className="rounded-2xl border-2 border-red-300 bg-red-50 p-6 shadow-sm">
                    <h3 className="m-0 text-base font-bold text-gray-900 mb-3">❌ Признаки минуса</h3>
                    <div className="mt-3">
                      {Array.isArray(item.deficitSignals) ? (
                        <CheckList list={item.deficitSignals} section="deficitSignals" />
                      ) : (
                        item.deficitSignals_text && (
                          <pre className="whitespace-pre-wrap text-sm leading-relaxed text-gray-900">{item.deficitSignals_text}</pre>
                        )
                      )}
                    </div>
                  </article>
                </div>
                </section>
            )}

            {/* Конфликты и проблемы */}
            {Array.isArray(item.conflicts) && item.conflicts.length > 0 && (
              <AccordionSection title="⚠️ Конфликты и проблемы" id="conflicts">
                <div className="space-y-6">
                  {item.conflicts.map((c: any, i: number) => (
                    <Entry key={i} title={c.title}>
                      {c.description && <p className="whitespace-pre-wrap">{c.description}</p>}
                      {Array.isArray(c.manifestations) && c.manifestations.length > 0 && (
                        <div>
                          <div className="mb-1 text-sm text-gray-500">Как проявляется</div>
                          <ul className="list-disc pl-6 space-y-1 text-gray-800">
                            {c.manifestations.map((m: string, j: number) => (
                              <li key={j}>{m}</li>
                            ))}
                          </ul>
                        </div>
                      )}
                      {c.advice && (
                        <div>
                          <div className="mb-1 text-sm text-gray-500">Что делать</div>
                          <p className="whitespace-pre-wrap">{c.advice}</p>
                        </div>
                      )}
                    </Entry>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Практики */}
            {item.practices && (
              <AccordionSection title="💡 Практики" id="practices">
                <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
                  {Object.entries(item.practices).map(([blockKey, list]: any, i: number) => (
                    <article key={i} className="rounded-2xl border-2 border-blue-200 bg-white p-4 shadow-sm">
                      <div className="mb-2 font-semibold text-gray-800">{mapPracticeKey(blockKey)}</div>
                      {Array.isArray(list) ? (
                        <div className="space-y-3">
                          {list.map((pr: any, j: number) => (
                            <Entry key={j} title={pr.title}>
                              {pr.p1 && <p className="text-sm whitespace-pre-wrap">{pr.p1}</p>}
                              {pr.p2 && <p className="text-sm whitespace-pre-wrap">{pr.p2}</p>}
                              {pr.description && <p className="text-sm whitespace-pre-wrap">{pr.description}</p>}
                            </Entry>
                          ))}
                        </div>
                      ) : (
                        <div className="text-sm text-gray-500">Нет данных</div>
                      )}
                    </article>
                  ))}
                </div>
              </AccordionSection>
            )}
          </div>
        ))}
      </div>
    );
  }

  // Функция рендеринга целевой консультации
  function renderTargetConsultation(
    items: any[],
    CheckList: any,
    Entry: any,
    checkedMap: Record<string, boolean>,
    setCheckedMap: (map: Record<string, boolean>) => void,
    localUiStateRef: React.MutableRefObject<Record<string, boolean>>,
    saveChecked: (map: Record<string, boolean>) => void
  ) {
    return (
      <div className="space-y-6">
        {items.map((item, idx) => (
          <div key={idx} className="space-y-6">
            {/* Цель клиента */}
            {item.request && (
              <section id="goal" className="rounded-2xl border-2 border-blue-200 bg-blue-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">🎯 Цель клиента</h2>
                <p className="mt-3 whitespace-pre-wrap leading-relaxed text-gray-800">{item.request}</p>
              </section>
            )}

            {/* Декомпозиция цели */}
            {Array.isArray(item.goalDecomposition) && item.goalDecomposition.length > 0 && (
              <section id="goal-decomposition" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">📋 Декомпозиция цели</h2>
                <ol className="space-y-3 list-none pl-0">
                  {item.goalDecomposition.map((goal: string, i: number) => (
                    <li key={i} className="text-gray-700 leading-relaxed flex gap-3">
                      <span className="font-semibold text-blue-600 flex-shrink-0">{i + 1}.</span>
                      <span>{goal}</span>
                    </li>
                  ))}
                </ol>
              </section>
            )}

            {/* Предупреждения */}
            {Array.isArray(item.warnings) && item.warnings.length > 0 && (
              <section id="warnings" className="rounded-2xl border-2 border-amber-200 bg-amber-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-amber-900 mb-3">⚠️ Предупреждения</h2>
                <ul className="space-y-2">
                  {item.warnings.map((w: string, i: number) => (
                    <li key={i} className="text-amber-800 leading-relaxed">• {w}</li>
                  ))}
                </ul>
              </section>
            )}

            {/* Вопросы для уточнения */}
            {Array.isArray(item.currentDiagnostics?.questions) && item.currentDiagnostics.questions.length > 0 && (
              <section id="questions" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">❓ Вопросы для уточнения</h2>
                <div className="space-y-3">
                  {item.currentDiagnostics.questions.map((q: any, i: number) => (
                    <div key={i} className="bg-blue-50 rounded-lg p-4 border border-blue-100">
                      <p className="text-gray-800 mb-1">{q.question}</p>
                      {q.salResource && (
                        <span className="text-xs text-gray-500">Ресурс: {q.salResource}</span>
                      )}
                    </div>
                  ))}
                </div>
              </section>
            )}

            {/* Диагностика */}
            {item.currentDiagnostics && (
              <AccordionSection title="🔍 Диагностика" id="diagnostics">
                <div className="space-y-6">
                  {Array.isArray(item.currentDiagnostics.resourceStates) && item.currentDiagnostics.resourceStates.length > 0 && (
                    <div>
                      <h3 className="text-base font-semibold mb-3">Состояние ресурсов</h3>
                      <div className="space-y-3">
                        {item.currentDiagnostics.resourceStates.map((state: any, i: number) => (
                          <div key={i} className={`rounded-lg p-4 border-2 ${
                            state.state === 'plus' ? 'border-green-200 bg-green-50' :
                            state.state === 'minus' ? 'border-red-200 bg-red-50' :
                            'border-gray-200 bg-gray-50'
                          }`}>
                            <div className="font-semibold mb-2">{state.resource}</div>
                            {Array.isArray(state.evidence) && state.evidence.length > 0 && (
                              <div className="mb-2">
                                <div className="text-xs font-semibold text-gray-600 mb-1">Доказательства:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.evidence.map((e: string, j: number) => (
                                    <li key={j}>{e}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                            {Array.isArray(state.correction) && state.correction.length > 0 && (
                              <div>
                                <div className="text-xs font-semibold text-gray-600 mb-1">Коррекция:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.correction.map((c: string, j: number) => (
                                    <li key={j}>{c}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                  {item.currentDiagnostics.readiness && (
                    <div className="rounded-lg border border-blue-100 p-4">
                      <h3 className="text-base font-semibold mb-2">Готовность</h3>
                      <div className="mb-2">
                        <span className="text-sm text-gray-600">Желание: </span>
                        <span className="font-semibold">{item.currentDiagnostics.readiness.willingness}/10</span>
                      </div>
                      {Array.isArray(item.currentDiagnostics.readiness.blockers) && item.currentDiagnostics.readiness.blockers.length > 0 && (
                        <div className="mb-2">
                          <div className="text-xs font-semibold text-gray-600 mb-1">Блокеры:</div>
                          <ul className="list-disc list-inside text-sm text-gray-700">
                            {item.currentDiagnostics.readiness.blockers.map((b: string, i: number) => (
                              <li key={i}>{b}</li>
                            ))}
                          </ul>
                        </div>
                      )}
                      {item.currentDiagnostics.readiness.comment && (
                        <p className="text-sm text-gray-700 mt-2">{item.currentDiagnostics.readiness.comment}</p>
                      )}
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Ресурсы по этапам */}
            {Array.isArray(item.resourcesForStages) && item.resourcesForStages.length > 0 && (
              <AccordionSection title="📊 Ресурсы по этапам" id="resources-stages">
                <div className="space-y-6">
                  {item.resourcesForStages.map((stage: any, stageIdx: number) => (
                    <div key={stageIdx} className="rounded-xl border border-blue-100 p-5">
                      <h3 className="text-lg font-semibold mb-4">Этап {stage.stage}</h3>
                      <div className="space-y-4">
                        {Array.isArray(stage.resources) && stage.resources.map((res: any, resIdx: number) => (
                          <div key={resIdx} className="bg-gray-50 rounded-lg p-4">
                            <div className="font-semibold text-gray-800 mb-2">{res.resource} {res.code && `(${res.code})`}</div>
                            {res.why && <p className="text-sm text-gray-600 mb-2">{res.why}</p>}
                            {Array.isArray(res.successSignals) && res.successSignals.length > 0 && (
                              <div className="mt-2">
                                <div className="text-xs font-semibold text-gray-500 uppercase mb-1">Признаки успеха:</div>
                                <ul className="list-disc list-inside space-y-1 text-sm text-gray-700">
                                  {res.successSignals.map((signal: string, sigIdx: number) => (
                                    <li key={sigIdx}>{signal}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* План действий */}
            {Array.isArray(item.plan123) && item.plan123.length > 0 && (
              <AccordionSection title="📝 План действий" id="plan">
                <div className="space-y-6">
                  {item.plan123.map((plan: any, i: number) => (
                    <div key={i} className="rounded-xl border-2 border-indigo-200 bg-indigo-50 p-5">
                      <h3 className="text-lg font-semibold mb-4">{plan.stageTitle}</h3>
                      {Array.isArray(plan.actions) && plan.actions.length > 0 && (
                        <div className="mb-4">
                          <div className="text-sm font-semibold text-gray-700 mb-2">Действия:</div>
                          <ul className="list-disc list-inside space-y-1 text-gray-700">
                            {plan.actions.map((action: string, j: number) => (
                              <li key={j}>{action}</li>
                            ))}
                          </ul>
                        </div>
                      )}
                      {Array.isArray(plan.resources) && plan.resources.length > 0 && (
                        <div className="mb-4">
                          <div className="text-sm font-semibold text-gray-700 mb-2">Ресурсы:</div>
                          <div className="flex flex-wrap gap-2">
                            {plan.resources.map((res: any, j: number) => {
                              // Убираем дубли цифр: если resource уже содержит код в скобках, не добавляем его снова
                              const resourceText = res.resource || '';
                              const hasCodeInText = /\(\d+\)/.test(resourceText);
                              const codeText = res.code && !hasCodeInText ? `(${res.code})` : '';
                              return (
                                <span key={j} className="px-3 py-1 bg-indigo-100 text-indigo-800 rounded-full text-sm">
                                  {resourceText} {codeText}
                                </span>
                              );
                            })}
                          </div>
                        </div>
                      )}
                      {plan.successCriteria && (
                        <div className="mb-4 p-3 bg-green-50 rounded-lg border border-green-200">
                          <div className="text-sm font-semibold text-green-800 mb-1">Критерий успеха:</div>
                          <p className="text-sm text-green-700">{plan.successCriteria}</p>
                        </div>
                      )}
                      {plan.riskNotes && (
                        <div className="p-3 bg-amber-50 rounded-lg border border-amber-200">
                          <div className="text-sm font-semibold text-amber-800 mb-1">⚠️ Риски:</div>
                          <p className="text-sm text-amber-700">{plan.riskNotes}</p>
                        </div>
                      )}
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Метрики прогресса */}
            {item.progressMetrics && (
              <AccordionSection title="📈 Метрики прогресса" id="metrics">
                <div className="space-y-4">
                  {Array.isArray(item.progressMetrics.earlySignals) && item.progressMetrics.earlySignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Ранние сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.earlySignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.progressMetrics.midSignals) && item.progressMetrics.midSignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Средние сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.midSignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.progressMetrics.resultSignals) && item.progressMetrics.resultSignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Итоговые сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.resultSignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Что делать в разных ситуациях */}
            {item.whatIf && (
              <AccordionSection title="🔄 Что делать в разных ситуациях" id="what-if">
                <div className="space-y-4">
                  {Array.isArray(item.whatIf.fatigue) && item.whatIf.fatigue.length > 0 && (
                    <div className="rounded-lg border border-orange-200 bg-orange-50 p-4">
                      <h3 className="text-sm font-semibold text-orange-900 mb-2">Усталость</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-orange-800">
                        {item.whatIf.fatigue.map((f: string, i: number) => (
                          <li key={i}>{f}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.overwhelm) && item.whatIf.overwhelm.length > 0 && (
                    <div className="rounded-lg border border-red-200 bg-red-50 p-4">
                      <h3 className="text-sm font-semibold text-red-900 mb-2">Перегрузка</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-red-800">
                        {item.whatIf.overwhelm.map((o: string, i: number) => (
                          <li key={i}>{o}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.relapse) && item.whatIf.relapse.length > 0 && (
                    <div className="rounded-lg border border-purple-200 bg-purple-50 p-4">
                      <h3 className="text-sm font-semibold text-purple-900 mb-2">Рецидив</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-purple-800">
                        {item.whatIf.relapse.map((r: string, i: number) => (
                          <li key={i}>{r}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.pitfalls) && item.whatIf.pitfalls.length > 0 && (
                    <div className="rounded-lg border border-amber-200 bg-amber-50 p-4">
                      <h3 className="text-sm font-semibold text-amber-900 mb-2">Ловушки</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-amber-800">
                        {item.whatIf.pitfalls.map((p: string, i: number) => (
                          <li key={i}>{p}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Обработка возражений */}
            {Array.isArray(item.objectionHandling) && item.objectionHandling.length > 0 && (
              <AccordionSection title="💬 Обработка возражений" id="objections">
                <div className="space-y-4">
                  {item.objectionHandling.map((obj: any, i: number) => (
                    <div key={i} className="rounded-lg border border-blue-100 bg-blue-50 p-4">
                      <div className="font-semibold text-gray-800 mb-2">&ldquo;{obj.objection}&rdquo;</div>
                      <p className="text-sm text-gray-700">{obj.reply}</p>
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Финальная стратегия */}
            {Array.isArray(item.finalStrategy) && item.finalStrategy.length > 0 && (
              <section id="strategy" className="rounded-2xl border-2 border-green-200 bg-green-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-green-900 mb-3">✅ Финальная стратегия</h2>
                <div className="space-y-3">
                  {item.finalStrategy.map((strategy: string, i: number) => (
                    <p key={i} className="text-green-800 leading-relaxed">{strategy}</p>
                  ))}
                </div>
              </section>
            )}
          </div>
        ))}
      </div>
    );
  }

  // Функция рендеринга партнерской консультации
  function renderPartnerConsultation(
    items: any[],
    CheckList: any,
    Entry: any,
    checkedMap: Record<string, boolean>,
    setCheckedMap: (map: Record<string, boolean>) => void,
    localUiStateRef: React.MutableRefObject<Record<string, boolean>>,
    saveChecked: (map: Record<string, boolean>) => void
  ) {
    return (
      <div className="space-y-6">
        {items.map((item, idx) => (
          <div key={idx} className="space-y-6">
            {/* Цель консультации */}
            {item.goal && (
              <section id="goal" className="rounded-2xl border-2 border-blue-200 bg-blue-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">🎯 Цель консультации</h2>
                <p className="mt-3 whitespace-pre-wrap leading-relaxed text-gray-800">{item.goal}</p>
              </section>
            )}

            {/* Предупреждения */}
            {Array.isArray(item.warnings) && item.warnings.length > 0 && (
              <section id="warnings" className="rounded-2xl border-2 border-amber-200 bg-amber-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-amber-900 mb-3">⚠️ Предупреждения</h2>
                <ul className="space-y-2">
                  {item.warnings.map((w: string, i: number) => (
                    <li key={i} className="text-amber-800 leading-relaxed">• {w}</li>
                  ))}
                </ul>
              </section>
            )}

            {/* Совместимость ресурсов */}
            {item.compatibility && (
              <AccordionSection title="🤝 Совместимость ресурсов" id="compatibility">
                <div className="space-y-6">
                  {Array.isArray(item.compatibility.complementary) && item.compatibility.complementary.length > 0 && (
                    <div>
                      <h3 className="text-base font-semibold text-green-800 mb-3">✅ Дополняющие ресурсы</h3>
                      <div className="space-y-3">
                        {item.compatibility.complementary.map((comp: any, i: number) => (
                          <div key={i} className="rounded-lg border border-green-200 bg-green-50 p-4">
                            <div className="font-semibold text-gray-800 mb-2">
                              {comp.firstResource} ↔ {comp.secondResource}
                            </div>
                            <p className="text-sm text-gray-700">{comp.why}</p>
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                  
                  {Array.isArray(item.compatibility.conflicts) && item.compatibility.conflicts.length > 0 && (
                    <div>
                      <h3 className="text-base font-semibold text-red-800 mb-3">⚠️ Конфликтующие ресурсы</h3>
                      <div className="space-y-3">
                        {item.compatibility.conflicts.map((conf: any, i: number) => (
                          <div key={i} className="rounded-lg border border-red-200 bg-red-50 p-4">
                            <div className="font-semibold text-gray-800 mb-2">
                              {conf.firstResource} ↔ {conf.secondResource}
                            </div>
                            <p className="text-sm text-gray-700 mb-2">{conf.why}</p>
                            {conf.solution && (
                              <div className="mt-2 pt-2 border-t border-red-300">
                                <div className="text-xs font-semibold text-red-800 mb-1">Решение:</div>
                                <p className="text-sm text-red-700">{conf.solution}</p>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Зоны конфликтов */}
            {Array.isArray(item.currentDiagnostics?.conflictZones) && item.currentDiagnostics.conflictZones.length > 0 && (
              <AccordionSection title="⚔️ Зоны конфликтов" id="conflict-zones">
                <div className="space-y-4">
                  {item.currentDiagnostics.conflictZones.map((zone: any, i: number) => (
                    <div key={i} className="rounded-lg border-2 border-amber-200 bg-amber-50 p-4">
                      <h3 className="text-base font-semibold text-amber-900 mb-3">{zone.zone}</h3>
                      <p className="text-sm text-gray-700 mb-3">{zone.description}</p>
                      <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mb-3">
                        <div className="bg-white rounded p-3 border border-amber-200">
                          <div className="text-xs font-semibold text-gray-600 mb-1">Первый участник:</div>
                          <p className="text-sm text-gray-800">{zone.firstParticipantRole}</p>
                        </div>
                        <div className="bg-white rounded p-3 border border-amber-200">
                          <div className="text-xs font-semibold text-gray-600 mb-1">Второй участник:</div>
                          <p className="text-sm text-gray-800">{zone.secondParticipantRole}</p>
                        </div>
                      </div>
                      {zone.solution && (
                        <div className="mt-3 pt-3 border-t border-amber-300">
                          <div className="text-xs font-semibold text-amber-800 mb-1">Решение:</div>
                          <p className="text-sm text-amber-700">{zone.solution}</p>
                        </div>
                      )}
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Диагностика */}
            {item.currentDiagnostics && !item.currentDiagnostics.firstParticipant && !item.currentDiagnostics.secondParticipant && (
              <AccordionSection title="🔍 Диагностика" id="diagnostics">
                <div className="space-y-6">
                  {Array.isArray(item.currentDiagnostics.resourceStates) && item.currentDiagnostics.resourceStates.length > 0 && (
                    <div>
                      <h3 className="text-base font-semibold mb-3">Состояние ресурсов</h3>
                      <div className="space-y-3">
                        {item.currentDiagnostics.resourceStates.map((state: any, i: number) => (
                          <div key={i} className={`rounded-lg p-4 border-2 ${
                            state.state === 'plus' ? 'border-green-200 bg-green-50' :
                            state.state === 'minus' ? 'border-red-200 bg-red-50' :
                            'border-gray-200 bg-gray-50'
                          }`}>
                            <div className="font-semibold mb-2">{state.resource}</div>
                            {Array.isArray(state.evidence) && state.evidence.length > 0 && (
                              <div className="mb-2">
                                <div className="text-xs font-semibold text-gray-600 mb-1">Доказательства:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.evidence.map((e: string, j: number) => (
                                    <li key={j}>{e}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                            {Array.isArray(state.correction) && state.correction.length > 0 && (
                              <div>
                                <div className="text-xs font-semibold text-gray-600 mb-1">Коррекция:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.correction.map((c: string, j: number) => (
                                    <li key={j}>{c}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                  {item.currentDiagnostics.readiness && (
                    <div className="rounded-lg border border-blue-100 p-4">
                      <h3 className="text-base font-semibold mb-2">Готовность</h3>
                      <div className="mb-2">
                        <span className="text-sm text-gray-600">Желание: </span>
                        <span className="font-semibold">{item.currentDiagnostics.readiness.willingness}/10</span>
                      </div>
                      {Array.isArray(item.currentDiagnostics.readiness.blockers) && item.currentDiagnostics.readiness.blockers.length > 0 && (
                        <div className="mb-2">
                          <div className="text-xs font-semibold text-gray-600 mb-1">Блокеры:</div>
                          <ul className="list-disc list-inside text-sm text-gray-700">
                            {item.currentDiagnostics.readiness.blockers.map((b: string, i: number) => (
                              <li key={i}>{b}</li>
                            ))}
                          </ul>
                        </div>
                      )}
                      {item.currentDiagnostics.readiness.comment && (
                        <p className="text-sm text-gray-700 mt-2">{item.currentDiagnostics.readiness.comment}</p>
                      )}
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Диагностика участников */}
            {(item.currentDiagnostics?.firstParticipant || item.currentDiagnostics?.secondParticipant) && (
              <AccordionSection title="👥 Диагностика участников" id="participants-diagnostics">
                <div className="space-y-6">
                  {/* Первый участник */}
                  {item.currentDiagnostics.firstParticipant && Array.isArray(item.currentDiagnostics.firstParticipant.resourceStates) && (
                    <div>
                      <h3 className="text-base font-semibold text-gray-800 mb-3">Первый участник</h3>
                      <div className="space-y-3">
                        {item.currentDiagnostics.firstParticipant.resourceStates.map((state: any, i: number) => (
                          <div key={i} className={`rounded-lg p-4 border-2 ${
                            state.state === 'plus' ? 'border-green-200 bg-green-50' :
                            state.state === 'minus' ? 'border-red-200 bg-red-50' :
                            'border-gray-200 bg-gray-50'
                          }`}>
                            <div className="font-semibold mb-2">{state.resource}</div>
                            {Array.isArray(state.evidence) && state.evidence.length > 0 && (
                              <div className="mb-2">
                                <div className="text-xs font-semibold text-gray-600 mb-1">Доказательства:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.evidence.map((e: string, j: number) => (
                                    <li key={j}>{e}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                            {Array.isArray(state.correction) && state.correction.length > 0 && (
                              <div>
                                <div className="text-xs font-semibold text-gray-600 mb-1">Коррекция:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.correction.map((c: string, j: number) => (
                                    <li key={j}>{c}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                  
                  {/* Второй участник */}
                  {item.currentDiagnostics.secondParticipant && Array.isArray(item.currentDiagnostics.secondParticipant.resourceStates) && (
                    <div>
                      <h3 className="text-base font-semibold text-gray-800 mb-3">Второй участник</h3>
                      <div className="space-y-3">
                        {item.currentDiagnostics.secondParticipant.resourceStates.map((state: any, i: number) => (
                          <div key={i} className={`rounded-lg p-4 border-2 ${
                            state.state === 'plus' ? 'border-green-200 bg-green-50' :
                            state.state === 'minus' ? 'border-red-200 bg-red-50' :
                            'border-gray-200 bg-gray-50'
                          }`}>
                            <div className="font-semibold mb-2">{state.resource}</div>
                            {Array.isArray(state.evidence) && state.evidence.length > 0 && (
                              <div className="mb-2">
                                <div className="text-xs font-semibold text-gray-600 mb-1">Доказательства:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.evidence.map((e: string, j: number) => (
                                    <li key={j}>{e}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                            {Array.isArray(state.correction) && state.correction.length > 0 && (
                              <div>
                                <div className="text-xs font-semibold text-gray-600 mb-1">Коррекция:</div>
                                <ul className="list-disc list-inside text-sm text-gray-700">
                                  {state.correction.map((c: string, j: number) => (
                                    <li key={j}>{c}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Вопросы для уточнения */}
            {Array.isArray(item.currentDiagnostics?.questions) && item.currentDiagnostics.questions.length > 0 && (
              <section id="questions" className="rounded-2xl border-2 border-blue-200 bg-white p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-gray-800 mb-3">❓ Вопросы для уточнения</h2>
                <div className="space-y-3">
                  {item.currentDiagnostics.questions.map((q: any, i: number) => (
                    <div key={i} className="bg-blue-50 rounded-lg p-4 border border-blue-100">
                      <p className="text-gray-800 mb-1">{q.question}</p>
                      <div className="flex items-center gap-2 mt-2">
                        {q.salResource && (
                          <span className="text-xs text-gray-500">Ресурс: {q.salResource}</span>
                        )}
                        {q.participant && (
                          <span className="text-xs text-gray-500">
                            {q.participant === 'first' ? 'Первый участник' : 
                             q.participant === 'second' ? 'Второй участник' : 
                             'Оба участника'}
                          </span>
                        )}
                      </div>
                    </div>
                  ))}
                </div>
              </section>
            )}

            {/* План действий */}
            {Array.isArray(item.plan123) && item.plan123.length > 0 && (
              <AccordionSection title="📝 План действий" id="plan">
                <div className="space-y-6">
                  {item.plan123.map((plan: any, i: number) => (
                    <div key={i} className="rounded-xl border-2 border-indigo-200 bg-indigo-50 p-5">
                      <h3 className="text-lg font-semibold mb-4">{plan.stageTitle}</h3>
                      {Array.isArray(plan.actions) && plan.actions.length > 0 && (
                        <div className="mb-4">
                          <div className="text-sm font-semibold text-gray-700 mb-2">Действия:</div>
                          <ul className="list-disc list-inside space-y-1 text-gray-700">
                            {plan.actions.map((action: string, j: number) => (
                              <li key={j}>{action}</li>
                            ))}
                          </ul>
                        </div>
                      )}
                      {Array.isArray(plan.resources) && plan.resources.length > 0 && (
                        <div className="mb-4">
                          <div className="text-sm font-semibold text-gray-700 mb-2">Ресурсы:</div>
                          <div className="flex flex-wrap gap-2">
                            {plan.resources.map((res: any, j: number) => {
                              // Убираем дубли цифр: если resource уже содержит код в скобках, не добавляем его снова
                              const resourceText = res.resource || '';
                              const hasCodeInText = /\(\d+\)/.test(resourceText);
                              const codeText = res.code && !hasCodeInText ? `(${res.code})` : '';
                              return (
                                <span key={j} className="px-3 py-1 bg-indigo-100 text-indigo-800 rounded-full text-sm">
                                  {resourceText} {codeText}
                                </span>
                              );
                            })}
                          </div>
                        </div>
                      )}
                      {plan.successCriteria && (
                        <div className="mb-4 p-3 bg-green-50 rounded-lg border border-green-200">
                          <div className="text-sm font-semibold text-green-800 mb-1">Критерий успеха:</div>
                          <p className="text-sm text-green-700">{plan.successCriteria}</p>
                        </div>
                      )}
                      {plan.riskNotes && (
                        <div className="p-3 bg-amber-50 rounded-lg border border-amber-200">
                          <div className="text-sm font-semibold text-amber-800 mb-1">⚠️ Риски:</div>
                          <p className="text-sm text-amber-700">{plan.riskNotes}</p>
                        </div>
                      )}
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Ресурсы по этапам */}
            {Array.isArray(item.resourcesForStages) && item.resourcesForStages.length > 0 && (
              <AccordionSection title="📊 Ресурсы по этапам" id="resources-stages">
                <div className="space-y-6">
                  {item.resourcesForStages.map((stage: any, stageIdx: number) => (
                    <div key={stageIdx} className="rounded-xl border border-blue-100 p-5">
                      <h3 className="text-lg font-semibold mb-4">Этап {stage.stage}</h3>
                      <div className="space-y-4">
                        {Array.isArray(stage.resources) && stage.resources.map((res: any, resIdx: number) => (
                          <div key={resIdx} className="bg-gray-50 rounded-lg p-4">
                            <div className="font-semibold text-gray-800 mb-2">
                              {res.resource} {res.code && `(${res.code})`}
                              {res.participant && (
                                <span className="text-sm font-normal text-gray-600 ml-2">
                                  ({res.participant === 'first' ? 'Первый участник' : 'Второй участник'})
                                </span>
                              )}
                            </div>
                            {res.why && <p className="text-sm text-gray-600 mb-2">{res.why}</p>}
                            {Array.isArray(res.successSignals) && res.successSignals.length > 0 && (
                              <div className="mt-2">
                                <div className="text-xs font-semibold text-gray-500 uppercase mb-1">Признаки успеха:</div>
                                <ul className="list-disc list-inside space-y-1 text-sm text-gray-700">
                                  {res.successSignals.map((signal: string, sigIdx: number) => (
                                    <li key={sigIdx}>{signal}</li>
                                  ))}
                                </ul>
                              </div>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Метрики прогресса */}
            {item.progressMetrics && (
              <AccordionSection title="📈 Метрики прогресса" id="metrics">
                <div className="space-y-4">
                  {Array.isArray(item.progressMetrics.earlySignals) && item.progressMetrics.earlySignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Ранние сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.earlySignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.progressMetrics.midSignals) && item.progressMetrics.midSignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Средние сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.midSignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.progressMetrics.resultSignals) && item.progressMetrics.resultSignals.length > 0 && (
                    <div>
                      <h3 className="text-sm font-semibold text-gray-700 mb-2">Итоговые сигналы</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-gray-600">
                        {item.progressMetrics.resultSignals.map((s: string, i: number) => (
                          <li key={i}>{s}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Что делать в разных ситуациях */}
            {item.whatIf && (
              <AccordionSection title="🔄 Что делать в разных ситуациях" id="what-if">
                <div className="space-y-4">
                  {Array.isArray(item.whatIf.fatigue) && item.whatIf.fatigue.length > 0 && (
                    <div className="rounded-lg border border-orange-200 bg-orange-50 p-4">
                      <h3 className="text-sm font-semibold text-orange-900 mb-2">Усталость</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-orange-800">
                        {item.whatIf.fatigue.map((f: string, i: number) => (
                          <li key={i}>{f}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.overwhelm) && item.whatIf.overwhelm.length > 0 && (
                    <div className="rounded-lg border border-red-200 bg-red-50 p-4">
                      <h3 className="text-sm font-semibold text-red-900 mb-2">Перегрузка</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-red-800">
                        {item.whatIf.overwhelm.map((o: string, i: number) => (
                          <li key={i}>{o}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.conflicts) && item.whatIf.conflicts.length > 0 && (
                    <div className="rounded-lg border border-purple-200 bg-purple-50 p-4">
                      <h3 className="text-sm font-semibold text-purple-900 mb-2">Конфликты</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-purple-800">
                        {item.whatIf.conflicts.map((c: string, i: number) => (
                          <li key={i}>{c}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.relapse) && item.whatIf.relapse.length > 0 && (
                    <div className="rounded-lg border border-purple-200 bg-purple-50 p-4">
                      <h3 className="text-sm font-semibold text-purple-900 mb-2">Рецидив</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-purple-800">
                        {item.whatIf.relapse.map((r: string, i: number) => (
                          <li key={i}>{r}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                  {Array.isArray(item.whatIf.pitfalls) && item.whatIf.pitfalls.length > 0 && (
                    <div className="rounded-lg border border-amber-200 bg-amber-50 p-4">
                      <h3 className="text-sm font-semibold text-amber-900 mb-2">Ловушки</h3>
                      <ul className="list-disc list-inside space-y-1 text-sm text-amber-800">
                        {item.whatIf.pitfalls.map((p: string, i: number) => (
                          <li key={i}>{p}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
              </AccordionSection>
            )}

            {/* Обработка возражений */}
            {Array.isArray(item.objectionHandling) && item.objectionHandling.length > 0 && (
              <AccordionSection title="💬 Обработка возражений" id="objections">
                <div className="space-y-4">
                  {item.objectionHandling.map((obj: any, i: number) => (
                    <div key={i} className="rounded-lg border border-blue-100 bg-blue-50 p-4">
                      <div className="font-semibold text-gray-800 mb-2">&ldquo;{obj.objection}&rdquo;</div>
                      <p className="text-sm text-gray-700">{obj.reply}</p>
                    </div>
                  ))}
                </div>
              </AccordionSection>
            )}

            {/* Финальная стратегия */}
            {Array.isArray(item.finalStrategy) && item.finalStrategy.length > 0 && (
              <section id="strategy" className="rounded-2xl border-2 border-green-200 bg-green-50 p-6 shadow-sm">
                <h2 className="m-0 text-base font-bold text-green-900 mb-3">✅ Финальная стратегия</h2>
                <div className="space-y-3">
                  {item.finalStrategy.map((strategy: string, i: number) => (
                    <p key={i} className="text-green-800 leading-relaxed">{strategy}</p>
                  ))}
                </div>
              </section>
            )}
          </div>
        ))}
      </div>
    );
  }

  // Название типа консультации
  const consultationTypeLabel = consultationType === "base" ? "Базовый" : 
                                  consultationType === "target" ? "Целевой" : 
                                  consultationType === "partner" ? "Партнерский" : 
                                  "Расчет";

  return (
    <div className="space-y-8 max-w-4xl mx-auto">
      {/* Общая шапка */}
      <div className="bg-white rounded-2xl border border-gray-200 p-6 shadow-sm">
        <div className="flex items-start justify-between gap-4">
          <div className="flex-1 min-w-0">
            <div className="text-2xl font-semibold text-gray-900 break-words">{clientName || "Профиль"}</div>
            <div className="flex items-center gap-4 mt-2 flex-wrap">
              {profile?.created_at && (
                <div className="text-sm text-gray-500">
                  <span className="font-medium">Дата создания:</span> {new Date(profile.created_at).toLocaleString("ru-RU")}
                </div>
              )}
              {consultationType && (
                <div className="text-sm">
                  <span className="text-gray-500 font-medium">Тип консультации:</span>{" "}
                  <span className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                    {consultationTypeLabel}
                  </span>
                </div>
              )}
            </div>
          </div>
          <ActionBar />
        </div>
      </div>

      {/* Пять кубиков (только для базового расчета) */}
      {consultationType === "base" && (() => {
        // digits: или массив, или строка JSON/CSV; возьмём как есть порядок
        let arr: any[] = [];
        const d = (profile as any)?.digits;
        if (Array.isArray(d)) arr = d;
        else if (typeof d === 'string') {
          try {
            const parsed = JSON.parse(d);
            if (Array.isArray(parsed)) arr = parsed;
            else arr = String(d).split(/[,\s]+/).filter(Boolean);
          } catch {
            arr = String(d).split(/[,\s]+/).filter(Boolean);
          }
        }
        return (
          <div className="flex flex-wrap gap-6 justify-center items-center py-4">
            {arr.slice(0,5).map((val: any, i: number) => (
              <div key={i} className="w-[74px] h-[74px] rounded-xl shadow-sm bg-[#1f92aa] text-white font-bold text-[28px] grid place-items-center">
                {val ?? ""}
              </div>
            ))}
          </div>
        );
      })()}

      {/* Пять изображений (только для базового расчета) */}
      {consultationType === "base" && (() => {
        const images = (profile as any)?.images;
        let imageArray: any[] = [];
        
        // Обработка разных форматов: массив файлов, один файл, строка JSON, или массив строк
        if (Array.isArray(images)) {
          imageArray = images;
        } else if (images) {
          if (typeof images === 'string') {
            try {
              const parsed = JSON.parse(images);
              imageArray = Array.isArray(parsed) ? parsed : [parsed];
            } catch {
              imageArray = [images];
            }
          } else {
            imageArray = [images];
          }
        }
        
        // Если нет изображений, показываем плейсхолдеры
        const displayImages = imageArray.length > 0 ? imageArray : Array.from({ length: 5 }).map(() => null);
        
        // Функция для получения URL изображения
        const getImageUrl = (img: any): string | null => {
          if (!img) return null;
          
          // Приоритет 1: Если есть прямой URL из S3 (новый формат)
          if (typeof img === 'object' && img.url) {
            return img.url;
          }
          
          // Приоритет 2: Если это обработанное изображение из коллекции images (с полями id и code)
          if (typeof img === 'object' && img.code) {
            // Используем code для получения изображения
            // Если code - это ID файла, используем /api/files/{code}
            if (/^\d+$/.test(String(img.code))) {
              return `/api/files/${img.code}`;
            }
            // Иначе code может быть URL или другим идентификатором
            return img.code;
          }
          
          // Приоритет 3: Если это объект файла Directus с полным URL
          if (typeof img === 'object') {
            if (img.id) {
              // Используем API прокси для изображений
              return `/api/files/${img.id}`;
            }
            if (img.filename_download) {
              return img.id ? `/api/files/${img.id}` : null;
            }
          }
          
          // Приоритет 4: Если это строка
          if (typeof img === 'string') {
            // Если уже полный URL
            if (img.startsWith('http')) return img;
            // Если это ID файла
            if (/^\d+$/.test(img)) return `/api/files/${img}`;
            // Иначе считаем это URL
            return img;
          }
          
          return null;
        };
        
        return (
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
            {displayImages.slice(0, 5).map((img: any, i: number) => {
              const imageUrl = getImageUrl(img);
              
              return (
                <div key={i} className="rounded-2xl overflow-hidden border bg-gray-50 relative" style={{ aspectRatio: '2/3' }}>
                  {imageUrl ? (
                    <>
                      {/* eslint-disable-next-line @next/next/no-img-element */}
                      <img 
                        src={imageUrl} 
                        alt={`Изображение ${i + 1}`}
                        className="w-full h-full object-contain"
                        onError={(e) => {
                          const target = e.target as HTMLImageElement;
                          console.error(`[ERROR] Failed to load image ${i + 1}:`, {
                            url: imageUrl,
                            imageData: img,
                            error: '403 Forbidden - возможно файл не загружен в S3 или bucket не публичный',
                            suggestion: 'Проверьте: 1) Файл загружен в S3 по пути sall_app/photo/{ID}.jpeg, 2) Bucket настроен как публичный'
                          });
                          target.style.display = 'none';
                          const placeholder = target.nextElementSibling as HTMLElement;
                          if (placeholder) {
                            placeholder.classList.remove('hidden');
                            placeholder.innerHTML = `<div class="text-red-400 text-xs text-center p-2">Ошибка 403<br/>Проверьте S3</div>`;
                          }
                        }}
                        onLoad={() => {
                          console.log(`[SUCCESS] Image ${i + 1} loaded successfully:`, imageUrl);
                        }}
                      />
                      <div className="hidden absolute inset-0 flex items-center justify-center text-gray-400 bg-gray-50">
            Изображение {i + 1}
          </div>
                    </>
                  ) : (
                    <div className="absolute inset-0 flex items-center justify-center text-gray-400">Изображение {i + 1}</div>
                  )}
      </div>
              );
            })}
      </div>
        );
      })()}

      {polling && <LoadingMessage />}

      {/* Меню-навигация по блокам */}
      {!polling && renderedFromJson && consultationType && (() => {
        const menuItems: Array<{ id: string; label: string; icon?: string }> = [];
        
        if (consultationType === "base") {
          menuItems.push(
            { id: "opener", label: "Скажите клиенту", icon: "❗" },
            { id: "personality", label: "Описание личности", icon: "👤" },
            { id: "strengths-weaknesses", label: "Сильные и слабые стороны", icon: "⚖️" },
            { id: "happiness", label: "Формула счастья", icon: "☺️" },
            { id: "codes", label: "Пояснение кодов", icon: "🔢" },
            { id: "diagnostics", label: "Диагностика ресурсов", icon: "🔍" },
            { id: "conflicts", label: "Конфликты и проблемы", icon: "⚠️" },
            { id: "practices", label: "Практики", icon: "💡" }
          );
        } else if (consultationType === "target") {
          menuItems.push(
            { id: "goal", label: "Цель клиента", icon: "🎯" },
            { id: "goal-decomposition", label: "Декомпозиция цели", icon: "📋" },
            { id: "warnings", label: "Предупреждения", icon: "⚠️" },
            { id: "questions", label: "Вопросы для уточнения", icon: "❓" },
            { id: "diagnostics", label: "Диагностика", icon: "🔍" },
            { id: "resources-stages", label: "Ресурсы по этапам", icon: "📊" },
            { id: "plan", label: "План действий", icon: "📝" },
            { id: "metrics", label: "Метрики прогресса", icon: "📈" },
            { id: "what-if", label: "Что делать в разных ситуациях", icon: "🔄" },
            { id: "objections", label: "Обработка возражений", icon: "💬" },
            { id: "strategy", label: "Финальная стратегия", icon: "✅" }
          );
        } else if (consultationType === "partner") {
          menuItems.push(
            { id: "goal", label: "Цель консультации", icon: "🎯" },
            { id: "warnings", label: "Предупреждения", icon: "⚠️" },
            { id: "compatibility", label: "Совместимость ресурсов", icon: "🤝" },
            { id: "conflict-zones", label: "Зоны конфликтов", icon: "⚔️" },
            { id: "diagnostics", label: "Диагностика", icon: "🔍" },
            { id: "participants-diagnostics", label: "Диагностика участников", icon: "👥" },
            { id: "questions", label: "Вопросы для уточнения", icon: "❓" },
            { id: "plan", label: "План действий", icon: "📝" },
            { id: "resources-stages", label: "Ресурсы по этапам", icon: "📊" },
            { id: "metrics", label: "Метрики прогресса", icon: "📈" },
            { id: "what-if", label: "Что делать в разных ситуациях", icon: "🔄" },
            { id: "objections", label: "Обработка возражений", icon: "💬" },
            { id: "strategy", label: "Финальная стратегия", icon: "✅" }
          );
        }

        if (menuItems.length === 0) return null;

        return (
          <div className="bg-white rounded-2xl border border-gray-200 shadow-sm sticky top-4 z-10">
            <button
              onClick={() => setNavigationExpanded(!navigationExpanded)}
              className="w-full flex items-center justify-between p-4 hover:bg-gray-50 transition-colors rounded-t-2xl"
            >
              <div className="text-sm font-semibold text-gray-700">Навигация по блокам</div>
              <svg 
                className={`w-5 h-5 text-gray-500 transition-transform ${navigationExpanded ? 'rotate-180' : ''}`}
                fill="none" 
                stroke="currentColor" 
                viewBox="0 0 24 24"
              >
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
              </svg>
            </button>
            {navigationExpanded && (
              <div className="p-4 pt-0 flex flex-wrap gap-2">
                {menuItems.map((item) => (
                  <a
                    key={item.id}
                    href={`#${item.id}`}
                    onClick={(e) => {
                      e.preventDefault();
                      const element = document.getElementById(item.id);
                      if (element) {
                        // Находим меню навигации для расчета offset
                        const menuElement = e.currentTarget.closest('.bg-white.rounded-2xl.border') as HTMLElement;
                        let offset = 100; // Дефолтный offset
                        
                        if (menuElement) {
                          const menuHeight = menuElement.offsetHeight;
                          // Высота меню + 10% от его высоты
                          offset = menuHeight + (menuHeight * 0.1);
                        }
                        
                        const elementPosition = element.getBoundingClientRect().top;
                        const offsetPosition = elementPosition + window.pageYOffset - offset;
                        window.scrollTo({
                          top: offsetPosition,
                          behavior: 'smooth'
                        });
                      }
                    }}
                    className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium bg-blue-50 text-blue-700 hover:bg-blue-100 transition-colors"
                  >
                    {item.icon && <span>{item.icon}</span>}
                    <span>{item.label}</span>
                  </a>
                ))}
              </div>
            )}
          </div>
        );
      })()}

      {/* Сообщение о необходимости перелогиниться */}
      {!polling && !profile && (
        <div className="card bg-red-50 border-red-200">
          <div className="flex items-center gap-3 text-red-800">
            <svg className="h-5 w-5 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
            <div>
              <div className="font-semibold">Сессия истекла</div>
              <div className="text-sm mt-1">Пожалуйста, перелогиньтесь для продолжения работы.</div>
              <a href="/login" className="text-sm underline mt-2 inline-block">Перейти на страницу входа</a>
            </div>
          </div>
        </div>
      )}

      {(() => {
        // Сначала проверяем HTML
        const html = (profile as any)?.html || (profile as any)?.raw_html || (profile as any)?.content || (profile as any)?.html_content;
        if (html && String(html).trim().length > 0) {
          return (
            <AccordionSection title="Готовый расчёт (HTML)">
              <div className="card" dangerouslySetInnerHTML={{ __html: String(html) }} />
            </AccordionSection>
          );
        }
        
        // Затем проверяем raw_json
        if (renderedFromJson) {
          return renderedFromJson;
        }
        
        // Если есть raw_json, но renderedFromJson вернул null, показываем отладочную информацию
        if (profile?.raw_json && !polling) {
          console.warn("[DEBUG] raw_json exists but renderedFromJson is null:", {
            rawJsonType: typeof profile.raw_json,
            rawJsonKeys: typeof profile.raw_json === 'object' && profile.raw_json !== null ? Object.keys(profile.raw_json) : [],
            rawJsonPreview: typeof profile.raw_json === 'string' 
              ? profile.raw_json.substring(0, 200) 
              : JSON.stringify(profile.raw_json).substring(0, 200)
          });
          
          // Пытаемся отобразить raw_json в сыром виде для отладки
          return (
            <div className="card">
              <div className="text-sm font-semibold mb-2">Данные из raw_json (отладка):</div>
              <pre className="text-xs bg-gray-100 p-3 rounded overflow-auto max-h-96">
                {typeof profile.raw_json === 'string' 
                  ? profile.raw_json 
                  : JSON.stringify(profile.raw_json, null, 2)}
              </pre>
            </div>
          );
        }
        
        return !polling ? (
          <div className="card text-sm text-gray-600">Нет данных для отображения.</div>
        ) : null;
      })()}

      {!polling && renderedFromJson && consultationType && (
        <div className="card space-y-4">
          <div className="font-medium">Задать вопрос по профилю</div>
          <div className="space-y-3">
            <div ref={chatBoxRef} className="rounded-xl border p-3 bg-white max-h-96 overflow-y-auto break-words">
            {chat.length === 0 && <div className="text-sm text-gray-500">Начните диалог — задайте вопрос ниже</div>}
            {chat.map((m, i) => (
              <div key={i} className={`mb-3 ${m.role === 'user' ? 'text-right' : 'text-left'}`}>
                <div className={`inline-block rounded-2xl px-4 py-3 max-w-[85%] ${m.role === 'user' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-800'}`}>
                  {m.role === 'assistant' ? (
                    <div className="prose prose-sm max-w-none dark:prose-invert">
                      <ReactMarkdown
                        remarkPlugins={[remarkGfm]}
                        components={{
                          // Заголовки
                          h1: ({node, ...props}) => <h1 className="text-lg font-bold mb-2 mt-3 first:mt-0" {...props} />,
                          h2: ({node, ...props}) => <h2 className="text-base font-bold mb-2 mt-3 first:mt-0" {...props} />,
                          h3: ({node, ...props}) => <h3 className="text-sm font-semibold mb-1 mt-2 first:mt-0" {...props} />,
                          // Параграфы
                          p: ({node, ...props}) => <p className="mb-2 last:mb-0 leading-relaxed" {...props} />,
                          // Списки
                          ul: ({node, ...props}) => <ul className="list-disc list-inside mb-2 space-y-1 ml-2" {...props} />,
                          ol: ({node, ...props}) => <ol className="list-decimal list-inside mb-2 space-y-1 ml-2" {...props} />,
                          li: ({node, ...props}) => <li className="leading-relaxed" {...props} />,
                          // Ссылки
                          a: ({node, ...props}) => <a className="text-blue-600 underline hover:text-blue-800" target="_blank" rel="noopener noreferrer" {...props} />,
                          // Код
                          code: ({node, inline, ...props}: any) => 
                            inline ? (
                              <code className="bg-gray-200 text-gray-800 px-1.5 py-0.5 rounded text-xs font-mono" {...props} />
                            ) : (
                              <code className="block bg-gray-200 text-gray-800 p-2 rounded text-xs font-mono overflow-x-auto mb-2" {...props} />
                            ),
                          pre: ({node, ...props}) => <pre className="bg-gray-200 p-2 rounded overflow-x-auto mb-2" {...props} />,
                          // Таблицы
                          table: ({node, ...props}) => (
                            <div className="overflow-x-auto my-2">
                              <table className="min-w-full border-collapse border border-gray-300" {...props} />
                            </div>
                          ),
                          thead: ({node, ...props}) => <thead className="bg-gray-200" {...props} />,
                          tbody: ({node, ...props}) => <tbody {...props} />,
                          tr: ({node, ...props}) => <tr className="border-b border-gray-300" {...props} />,
                          th: ({node, ...props}) => <th className="border border-gray-300 px-2 py-1 text-left font-semibold" {...props} />,
                          td: ({node, ...props}) => <td className="border border-gray-300 px-2 py-1" {...props} />,
                          // Выделение
                          strong: ({node, ...props}) => <strong className="font-semibold" {...props} />,
                          em: ({node, ...props}) => <em className="italic" {...props} />,
                          // Горизонтальная линия
                          hr: ({node, ...props}) => <hr className="my-3 border-gray-300" {...props} />,
                          // Блоки цитат
                          blockquote: ({node, ...props}) => (
                            <blockquote className="border-l-4 border-gray-400 pl-3 italic my-2 text-gray-700" {...props} />
                          ),
                        }}
                      >
                        {m.content}
                      </ReactMarkdown>
                    </div>
                  ) : (
                    <div className="whitespace-pre-wrap break-words">{m.content}</div>
                  )}
                  {loading && i === chat.length - 1 && m.role === 'assistant' && (
                    <span className="inline-block w-2 h-4 ml-1 bg-gray-400 animate-pulse">▋</span>
                  )}
                </div>
              </div>
            ))}
          </div>
          <form onSubmit={ask} className="flex items-start gap-3">
            <textarea
              className="flex-1 rounded-xl border p-3 h-24 resize-none overflow-y-auto"
              value={question}
              onChange={(e) => setQuestion(e.target.value)}
              placeholder="Например: как лучше работать с конфликтом 2↔7?"
              required
            />
            <button
              disabled={loading}
              className="rounded-2xl bg-brand-600 text-white px-4 py-2 h-11 mt-1 hover:bg-brand-700 disabled:opacity-60"
            >
              {loading ? "Думаю..." : "Спросить"}
            </button>
          </form>
        </div>
        </div>
      )}

      {/* Плавающая кнопка «Заметки» */}
      <button
        onClick={() => setNotesOpen(true)}
        className="fixed right-6 bottom-6 rounded-full shadow-lg bg-brand-600 text-white px-5 py-3 hover:bg-brand-700 z-40"
      >
        📝 Заметки
      </button>

      {notesOpen && (
        <div className="fixed inset-0 bg-black/40 z-50 grid place-items-center p-4" onClick={() => setNotesOpen(false)}>
          <div className="bg-white rounded-2xl shadow-xl max-w-5xl w-full max-h-[90vh] overflow-hidden flex flex-col" onClick={(e)=>e.stopPropagation()}>
            <div className="flex items-center justify-between p-6 border-b">
              <div className="font-semibold">Заметки по расчёту</div>
              <button className="text-gray-500 hover:text-gray-800" onClick={() => setNotesOpen(false)}>✕</button>
            </div>
            <div className="p-6 overflow-y-auto flex-grow">
            {/* Шаблоны заметок */}
            <div className="flex flex-wrap gap-2 pb-2 border-b mb-4">
              <span className="text-xs text-gray-500 mr-2">Шаблоны:</span>
              <button
                className="rounded border px-2 py-1 text-xs hover:bg-gray-50"
                onClick={(e) => {
                  e.preventDefault();
                  const template = `## Встреча ${new Date().toLocaleDateString('ru-RU')}\n\n### Обсудили:\n- \n- \n\n### Действия:\n- \n- \n\n### Следующая встреча:\n`;
                  setNotesDraft(notesDraft + (notesDraft ? '\n\n' : '') + template);
                  notesTouchedRef.current = true;
                }}
              >
                📅 Встреча
              </button>
              <button
                className="rounded border px-2 py-1 text-xs hover:bg-gray-50"
                onClick={(e) => {
                  e.preventDefault();
                  const template = `## Заметки\n\n### Сильные стороны:\n- \n- \n\n### Области роста:\n- \n- \n\n### Рекомендации:\n- \n- \n`;
                  setNotesDraft(notesDraft + (notesDraft ? '\n\n' : '') + template);
                  notesTouchedRef.current = true;
                }}
              >
                💡 Анализ
              </button>
              <button
                className="rounded border px-2 py-1 text-xs hover:bg-gray-50"
                onClick={(e) => {
                  e.preventDefault();
                  const template = `## Задачи\n\n- [ ] \n- [ ] \n- [ ] \n`;
                  setNotesDraft(notesDraft + (notesDraft ? '\n\n' : '') + template);
                  notesTouchedRef.current = true;
                }}
              >
                ✅ Задачи
              </button>
            </div>
            
            {/* Мини-редактор markdown */}
            <div className="flex flex-wrap gap-2 mb-2">
              <button className="rounded border px-2 py-1 text-sm hover:bg-gray-50" onClick={(e)=>{e.preventDefault(); wrapSelection('**');}}>B</button>
              <button className="rounded border px-2 py-1 text-sm hover:bg-gray-50" onClick={(e)=>{e.preventDefault(); wrapSelection('*');}}>I</button>
              <button className="rounded border px-2 py-1 text-sm hover:bg-gray-50" onClick={(e)=>{e.preventDefault(); prefixLines('## ');}}>H2</button>
              <button className="rounded border px-2 py-1 text-sm hover:bg-gray-50" onClick={(e)=>{e.preventDefault(); prefixLines('- ');}}>• Список</button>
            </div>
            <div className="grid grid-cols-2 gap-4">
              <div>
                <div className="text-sm font-medium text-gray-700 mb-2">Редактирование (Markdown)</div>
                <textarea
                  ref={notesTextareaRef}
                  className="w-full h-64 rounded-xl border p-3 font-mono text-sm"
                  value={notesDraft}
                  onChange={(e)=>{ setNotesDraft(e.target.value); notesTouchedRef.current = true; }}
                  placeholder="Markdown поддерживается. Используйте **жирный**, *курсив*, ## заголовки, - списки"
                />
              </div>
              <div>
                <div className="text-sm font-medium text-gray-700 mb-2">Предпросмотр</div>
                <div className="w-full h-64 rounded-xl border p-3 bg-gray-50 overflow-y-auto prose prose-sm max-w-none">
                  {notesDraft ? (
                    <ReactMarkdown
                      remarkPlugins={[remarkGfm]}
                      components={{
                        h1: ({node, ...props}) => <h1 className="text-lg font-bold mb-2 mt-3 first:mt-0" {...props} />,
                        h2: ({node, ...props}) => <h2 className="text-base font-bold mb-2 mt-3 first:mt-0" {...props} />,
                        h3: ({node, ...props}) => <h3 className="text-sm font-semibold mb-1 mt-2 first:mt-0" {...props} />,
                        p: ({node, ...props}) => <p className="mb-2 last:mb-0 leading-relaxed" {...props} />,
                        ul: ({node, ...props}) => <ul className="list-disc list-inside mb-2 space-y-1 ml-2" {...props} />,
                        ol: ({node, ...props}) => <ol className="list-decimal list-inside mb-2 space-y-1 ml-2" {...props} />,
                        li: ({node, ...props}) => <li className="leading-relaxed" {...props} />,
                        strong: ({node, ...props}) => <strong className="font-semibold" {...props} />,
                        em: ({node, ...props}) => <em className="italic" {...props} />,
                        code: ({node, inline, ...props}: any) => 
                          inline ? (
                            <code className="bg-gray-200 text-gray-800 px-1.5 py-0.5 rounded text-xs font-mono" {...props} />
                          ) : (
                            <code className="block bg-gray-200 text-gray-800 p-2 rounded text-xs font-mono overflow-x-auto mb-2" {...props} />
                          ),
                      }}
                    >
                      {notesDraft}
                    </ReactMarkdown>
                  ) : (
                    <div className="text-sm text-gray-400 italic">Предпросмотр появится здесь</div>
                  )}
                </div>
              </div>
            </div>
            <div className="flex gap-2 justify-end mt-4">
                <button className="rounded-xl border px-4 py-2" onClick={()=>setNotesOpen(false)}>Отмена</button>
                <button
                  className="rounded-xl bg-brand-600 text-white px-4 py-2 hover:bg-brand-700 disabled:opacity-60"
                  disabled={savingNotes}
                  onClick={async()=>{
                    setSavingNotes(true);
                    try {
                      const res = await fetch(`/api/profiles/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ notes: notesDraft }) });
                      if (!res.ok) throw new Error('Failed to save notes');
                      setProfile(p => (p ? { ...p, notes: notesDraft } : p));
                      setNotesOpen(false);
                    } finally { setSavingNotes(false); }
                  }}
                >
                  {savingNotes ? 'Сохранение...' : 'Сохранить'}
                </button>
              </div>
          </div>
        </div>
      )}
    </div>
  );
}

function mapPracticeKey(key: string): string {
  const dict: Record<string, string> = {
    personality: "Личность",
    connector: "Связи/Коммуникация",
    realization: "Реализация",
    generator: "Источник/Подпитка",
    mission: "Миссия",
  };
  return dict[key] || key;
}
