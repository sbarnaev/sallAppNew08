# Чеклист деплоя системы одноразовых ссылок на тесты

## ✅ Перед деплоем

### 1. Запуск скрипта настройки коллекции

```bash
DIRECTUS_URL="https://ваш-directus-url.com" \
DIRECTUS_ADMIN_TOKEN="ваш-админский-токен" \
node scripts/setup-test-tokens.mjs
```

**Проверьте:** Скрипт должен завершиться без ошибок и создать коллекцию `test_tokens`

### 2. Настройка переменных окружения

Добавьте в переменные окружения на сервере:

```env
NEXT_PUBLIC_APP_URL=https://ваш-домен.com
DIRECTUS_SERVICE_TOKEN=ваш-статический-токен-directus
```

**Проверьте:**
- `NEXT_PUBLIC_APP_URL` должен быть полным URL без слеша в конце
- `DIRECTUS_SERVICE_TOKEN` должен быть валидным статическим токеном

### 3. Настройка прав доступа в Directus

В админ-панели Directus:

**Для сервисного токена (DIRECTUS_SERVICE_TOKEN):**
- Коллекция `test_tokens`: Чтение (Read) - все записи
- Коллекция `test_tokens`: Обновление (Update) - все записи
- Коллекция `clients`: Чтение (Read) - все записи
- Коллекция `clients`: Обновление (Update) - все записи (только поле `testirovanie`)

**Для роли пользователей (например, "master"):**
- Коллекция `test_tokens`: Создание (Create) - свои записи
- Коллекция `test_tokens`: Чтение (Read) - свои записи

### 4. Проверка файлов

Убедитесь, что все файлы на месте:

**API маршруты:**
- ✅ `app/api/tests/generate-link/route.ts`
- ✅ `app/api/tests/verify-token/route.ts`
- ✅ `app/api/tests/submit-by-token/route.ts`

**Публичные страницы:**
- ✅ `app/test/[token]/page.tsx`

**Компоненты:**
- ✅ `components/TestLinkGenerator.tsx`

**Обновленные файлы:**
- ✅ `components/ClientTestResults.tsx`
- ✅ `app/(protected)/tests/page.tsx`

**Скрипты:**
- ✅ `scripts/setup-test-tokens.mjs`

**Документация:**
- ✅ `docs/TEST_LINKS_SETUP.md`

## 🧪 Тестирование после деплоя

### 1. Тест генерации ссылки

1. Войдите в систему
2. Откройте страницу клиента
3. В разделе "Результаты тестирования" нажмите "Получить ссылку для клиента"
4. **Ожидаемый результат:** Появится ссылка, которую можно скопировать

### 2. Тест публичного прохождения теста

1. Скопируйте ссылку из предыдущего шага
2. Откройте ссылку в режиме инкогнито (без авторизации)
3. **Ожидаемый результат:** 
   - Загружается страница теста
   - Отображается название теста и имя клиента
   - Можно пройти тест

### 3. Тест сохранения результата

1. Пройдите тест полностью
2. Нажмите "Завершить"
3. **Ожидаемый результат:**
   - Отображается результат теста
   - Появляется сообщение "Результат успешно сохранен"
   - Результат виден на странице клиента в разделе "Результаты тестирования"

### 4. Тест одноразовости ссылки

1. Попробуйте открыть ту же ссылку еще раз
2. **Ожидаемый результат:** 
   - Показывается ошибка "Токен недействителен, использован или истек срок действия"

### 5. Тест валидации

1. Попробуйте открыть несуществующий токен: `/test/invalid-token-123`
2. **Ожидаемый результат:** Показывается ошибка "Invalid token"

## 🐛 Возможные проблемы

### Ошибка "Service token not configured"

**Причина:** Не установлена переменная `DIRECTUS_SERVICE_TOKEN`

**Решение:** Проверьте переменные окружения на сервере

### Ошибка "Failed to create test token"

**Причина:** 
- Нет прав на создание записей в `test_tokens`
- Коллекция не создана

**Решение:**
- Проверьте права доступа в Directus
- Запустите скрипт настройки коллекции

### Ошибка "Failed to verify token"

**Причина:** 
- Сервисный токен не имеет прав на чтение `test_tokens`
- Коллекция не создана

**Решение:**
- Проверьте права сервисного токена
- Убедитесь, что коллекция создана

### Ссылка не работает / 404

**Причина:** 
- Неправильный `NEXT_PUBLIC_APP_URL`
- Маршрут `/test/[token]` не доступен

**Решение:**
- Проверьте `NEXT_PUBLIC_APP_URL` (должен быть без слеша в конце)
- Пересоберите приложение после изменения переменных окружения

### Результат не сохраняется

**Причина:**
- Сервисный токен не имеет прав на обновление `clients`
- Ошибка при сохранении

**Решение:**
- Проверьте права сервисного токена
- Проверьте логи сервера

## 📝 Логи для отладки

В логах сервера ищите:
- `Error generating test link` - ошибки при генерации ссылки
- `Error verifying token` - ошибки при проверке токена
- `Error submitting test result by token` - ошибки при сохранении результата
- `Failed to create test token` - ошибки создания токена
- `Failed to mark token as used` - предупреждение (не критично, если результат сохранен)

## ✅ После успешного тестирования

1. Убедитесь, что все тесты прошли успешно
2. Проверьте логи на наличие ошибок
3. Сообщите пользователям о новой возможности
