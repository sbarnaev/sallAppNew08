# Стратегия разработки новых функций

## 🎯 Основная идея

Мы работаем так, чтобы:
- ✅ Разрабатывать новые функции безопасно, не ломая то, что уже работает
- ✅ Исправлять баги и дорабатывать существующий функционал в любой момент
- ✅ Тестировать новое на отдельном сервере перед выпуском
- ✅ Легко откатывать изменения, если что-то пошло не так

---

## 📚 Основные понятия

### Ветка (Branch)
Это как отдельная копия проекта, где ты можешь работать независимо от основной версии. Изменения в одной ветке не влияют на другую, пока ты их не объединишь.

### Main (основная ветка)
Это рабочая версия, которая работает у всех пользователей. Сюда попадает только проверенный и протестированный код.

### Develop (ветка разработки)
Это промежуточная версия для тестирования. Сюда попадают новые функции перед тем, как их выпустить в main.

### Feature (ветка функции)
Это временная ветка для разработки конкретной новой функции. Когда функция готова, ветку объединяют с develop, а потом удаляют.

### Hotfix (ветка исправления)
Это срочная ветка для исправления критических багов в main. Исправления сразу попадают в main, а потом переносятся в develop.

---

## 🔄 Процесс работы

### Сценарий 1: Разработка новой функции

**Пример:** Разработка блока консультационных скриптов

1. **Создание ветки**
   - От main создаешь новую ветку `feature/consultation-scripts`
   - В этой ветке работаешь над новой функцией
   - Main остается нетронутым, пользователи работают как обычно

2. **Разработка**
   - Разрабатываешь функцию в своей ветке
   - Тестируешь локально на своем компьютере
   - Коммитишь изменения в свою ветку

3. **Тестирование на staging**
   - Когда функция готова, объединяешь ветку с develop
   - Develop автоматически деплоится на staging сервер (копия production)
   - Тестируешь там все сценарии
   - Если нашел баги - исправляешь в той же ветке и снова мерджишь в develop

4. **Выпуск в production**
   - Когда все протестировано и работает, объединяешь develop с main
   - Main автоматически деплоится на production
   - Пользователи получают новую функцию

**Важно:** Пока ты работаешь в feature ветке, main остается стабильным, и ты можешь в любой момент исправить что-то в main, не затрагивая свою разработку.

---

### Сценарий 2: Исправление бага в существующем функционале

**Пример:** Нашел ошибку в форме создания клиента, пока разрабатываешь консультационные скрипты

1. **Переключение на main**
   - Переключаешься на ветку main
   - Создаешь ветку `hotfix/fix-client-form` от main
   - Исправляешь баг в этой ветке

2. **Быстрый деплой**
   - Объединяешь hotfix ветку с main
   - Main деплоится на production
   - Пользователи получают исправление

3. **Синхронизация с разработкой**
   - Объединяешь main с develop (чтобы develop тоже получил исправление)
   - Объединяешь develop с твоей feature веткой (чтобы твоя разработка тоже получила исправление)
   - Продолжаешь разработку новой функции

**Результат:** Баг исправлен для пользователей, и твоя разработка тоже получила это исправление.

---

## 🗂️ Структура веток

```
main (production) ←─── пользователи работают здесь
  │
  ├─── hotfix/fix-client-form ←─── срочное исправление
  │         │
  │         └───→ main (после исправления)
  │
  └─── develop (staging) ←─── тестирование
         │
         └─── feature/consultation-scripts ←─── разработка новой функции
                   │
                   └───→ develop (после тестирования)
                            │
                            └───→ main (после финального тестирования)
```

---

## 🎛️ Feature Flags (флаги функций)

### Что это?
Это переключатель, который позволяет включать/выключать новую функцию без изменения кода. Функция уже в коде, но скрыта от пользователей.

### Как работает?

1. **В коде**
   - Новая функция обернута проверкой флага
   - Если флаг выключен - функция не показывается
   - Если флаг включен - функция работает

2. **В настройках**
   - Флаг хранится в переменных окружения (`.env`)
   - На staging можешь включить для тестирования
   - На production можешь включить только для себя или постепенно для всех

3. **Преимущества**
   - Можно включить функцию только для себя для тестирования
   - Можно быстро выключить, если что-то не так
   - Можно включать постепенно (10% пользователей, потом 50%, потом все)

### Пример использования

**Разработка:** Функция готова, но флаг выключен → никто не видит
**Staging:** Включаешь флаг → тестируешь
**Production (этап 1):** Включаешь флаг только для себя → проверяешь
**Production (этап 2):** Включаешь для 10% пользователей → смотришь метрики
**Production (этап 3):** Включаешь для всех → функция доступна всем

---

## 🗄️ Работа с базой данных

### Твоя ситуация
- Directus подключен по URL
- Это внешний сервис, не локальная БД
- Не нужно дампить и восстанавливать БД

### Как работать?

**Для staging сервера:**
- Можешь использовать тот же Directus (по URL)
- Или создать отдельный Directus для staging (если нужно)
- В `.env.staging` указываешь URL staging Directus (или тот же production)

**Важно:**
- Если используешь тот же Directus - будь осторожен с миграциями
- Лучше использовать отдельный Directus для staging, если это возможно
- Или тестировать миграции на копии данных

**Для разработки:**
- Локально можешь использовать production Directus (только для чтения)
- Или свой локальный Directus для полного тестирования

---

## 📋 Типичные ситуации

### Ситуация 1: Разрабатываю новую функцию, но нужно срочно исправить баг

**Решение:**
1. Сохраняешь текущие изменения в feature ветке (коммит)
2. Переключаешься на main
3. Создаешь hotfix ветку от main
4. Исправляешь баг
5. Мерджишь hotfix в main → деплой
6. Мерджишь main в develop
7. Мерджишь develop в свою feature ветку
8. Возвращаешься к разработке

**Результат:** Баг исправлен, разработка продолжается с учетом исправления.

---

### Ситуация 2: Новая функция почти готова, но нужно доработать существующий функционал

**Решение:**
1. Доработка идет в отдельной feature ветке от main
2. Или в hotfix ветке, если это срочно
3. После деплоя доработки - мерджишь изменения в свою основную feature ветку

**Результат:** Обе задачи решены, ничего не потеряно.

---

### Ситуация 3: Нашел критический баг в новой функции на staging

**Решение:**
1. Исправляешь в той же feature ветке
2. Коммитишь исправление
3. Мерджишь в develop
4. Тестируешь снова на staging
5. Когда все ок - мерджишь в main

**Результат:** Баг исправлен до выпуска в production.

---

### Ситуация 4: Новая функция работает на staging, но нужно еще подумать

**Решение:**
1. Оставляешь функцию в develop (она на staging)
2. Можешь тестировать сколько нужно
3. Когда готов - мерджишь в main
4. Или откатываешь, если передумал (удаляешь из develop)

**Результат:** Никакого давления, можешь тестировать сколько угодно.

---

## 🚀 Рекомендуемый workflow

### Ежедневная работа

1. **Утром:** Обновляешь свои ветки из main/develop
   - Это гарантирует, что у тебя последние исправления

2. **В течение дня:** Работаешь в своей feature ветке
   - Коммитишь изменения по мере готовности

3. **Перед деплоем:** Тестируешь локально
   - Проверяешь, что все работает

4. **После тестирования:** Мерджишь в develop
   - Автоматический деплой на staging
   - Тестируешь там

5. **После финального тестирования:** Мерджишь в main
   - Автоматический деплой на production

---

## ⚠️ Важные правила

1. **Никогда не коммить напрямую в main**
   - Всегда через ветки
   - Main = только проверенный код

2. **Всегда тестируй на staging перед production**
   - Staging = копия production
   - Если работает на staging, скорее всего работает и на production

3. **Используй feature flags для новых функций**
   - Можно быстро выключить при проблемах
   - Можно включать постепенно

4. **Регулярно синхронизируй ветки**
   - Мерджи main → develop → feature ветки
   - Это гарантирует, что у тебя последние исправления

5. **Документируй изменения**
   - Коммиты с понятными сообщениями
   - CHANGELOG для пользователей

---

## 🎯 Итоговая схема работы

```
[Новая функция]
    ↓
feature/новая-функция (разработка)
    ↓
develop (staging, тестирование)
    ↓
main (production, выпуск)

[Исправление бага]
    ↓
hotfix/исправление (от main)
    ↓
main (быстрый деплой)
    ↓
develop (синхронизация)
    ↓
feature ветки (синхронизация)
```

---

## 💡 Советы

1. **Одна ветка = одна функция**
   - Не смешивай несколько функций в одной ветке
   - Легче управлять и откатывать

2. **Регулярные коммиты**
   - Коммить часто, небольшими порциями
   - Легче найти, где что-то пошло не так

3. **Понятные названия веток**
   - `feature/consultation-scripts` - понятно
   - `feature/stuff` - непонятно

4. **Тестируй перед мерджем**
   - Всегда проверяй локально
   - Всегда проверяй на staging

5. **Не бойся откатывать**
   - Если что-то не так - откатывай
   - Лучше откатить, чем выпустить баг

---

## 📝 Чеклист перед деплоем в production

- [ ] Функция протестирована локально
- [ ] Функция протестирована на staging
- [ ] Нет ошибок линтера/TypeScript
- [ ] Проверено на мобильных устройствах
- [ ] Нет конфликтов с существующим функционалом
- [ ] Feature flag настроен (если нужен)
- [ ] Документация обновлена
- [ ] Готов к откату (знаешь, как откатить)

---

Этот процесс гарантирует, что ты можешь безопасно разрабатывать новое, не ломая существующее, и всегда можешь быстро исправить проблемы.

